"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.isDeclaration = exports.Declaration = exports.ParseJsonSchemaId = exports.getJSONIdentifier = exports.getJSONIdentifierAnnotation = exports.JSONIdentifierAnnotationId = exports.hasStableFilter = exports.getSurrogateAnnotation = exports.getDecodingFallbackAnnotation = exports.getParseOptionsAnnotation = exports.getParseIssueTitleAnnotation = exports.getBatchingAnnotation = exports.getConcurrencyAnnotation = exports.getDocumentationAnnotation = exports.getJSONSchemaAnnotation = exports.getDefaultAnnotation = exports.getExamplesAnnotation = exports.getDescriptionAnnotation = exports.getIdentifierAnnotation = exports.getAutoTitleAnnotation = exports.getTitleAnnotation = exports.getMissingMessageAnnotation = exports.getMessageAnnotation = exports.getSchemaIdAnnotation = exports.getBrandAnnotation = exports.getTypeConstructorAnnotation = exports.getAnnotation = exports.StableFilterAnnotationId = exports.SurrogateAnnotationId = exports.DecodingFallbackAnnotationId = exports.ParseOptionsAnnotationId = exports.ParseIssueTitleAnnotationId = exports.BatchingAnnotationId = exports.ConcurrencyAnnotationId = exports.DocumentationAnnotationId = exports.EquivalenceAnnotationId = exports.PrettyAnnotationId = exports.ArbitraryAnnotationId = exports.JSONSchemaAnnotationId = exports.DefaultAnnotationId = exports.ExamplesAnnotationId = exports.DescriptionAnnotationId = exports.AutoTitleAnnotationId = exports.TitleAnnotationId = exports.IdentifierAnnotationId = exports.MissingMessageAnnotationId = exports.MessageAnnotationId = exports.SchemaIdAnnotationId = exports.BrandAnnotationId = exports.TypeConstructorAnnotationId = void 0;
exports.IndexSignature = exports.isParameter = exports.PropertySignature = exports.isTupleType = exports.TupleType = exports.OptionalType = exports.Type = exports.isTemplateLiteral = exports.TemplateLiteral = exports.TemplateLiteralSpan = exports.isEnums = exports.Enums = exports.isObjectKeyword = exports.objectKeyword = exports.ObjectKeyword = exports.isSymbolKeyword = exports.symbolKeyword = exports.SymbolKeyword = exports.isBigIntKeyword = exports.bigIntKeyword = exports.BigIntKeyword = exports.isBooleanKeyword = exports.booleanKeyword = exports.BooleanKeyword = exports.isNumberKeyword = exports.numberKeyword = exports.NumberKeyword = exports.isStringKeyword = exports.stringKeyword = exports.StringKeyword = exports.isAnyKeyword = exports.anyKeyword = exports.AnyKeyword = exports.isUnknownKeyword = exports.unknownKeyword = exports.UnknownKeyword = exports.isNeverKeyword = exports.neverKeyword = exports.NeverKeyword = exports.isVoidKeyword = exports.voidKeyword = exports.VoidKeyword = exports.isUndefinedKeyword = exports.undefinedKeyword = exports.UndefinedKeyword = exports.isUniqueSymbol = exports.UniqueSymbol = exports.null = exports.isLiteral = exports.Literal = void 0;
exports.pruneUndefined = exports.rename = exports.compose = exports.equals = exports.getEncodedParameter = exports.encodedBoundAST = exports.encodedAST = exports.getTransformationFrom = exports.typeAST = exports.omitAnnotations = exports.pickAnnotations = exports.getCompiler = exports.mutable = exports.required = exports.partial = exports.orUndefined = exports.omit = exports.pick = exports.record = exports.getPropertyKeyIndexedAccess = exports.getNumberIndexedAccess = exports.getPropertySignatures = exports.getTemplateLiteralCapturingRegExp = exports.getTemplateLiteralRegExp = exports.keyof = exports.annotations = exports.isTypeLiteralTransformation = exports.TypeLiteralTransformation = exports.PropertySignatureTransformation = exports.isComposeTransformation = exports.composeTransformation = exports.ComposeTransformation = exports.isFinalTransformation = exports.FinalTransformation = exports.isTransformation = exports.Transformation = exports.defaultParseOption = exports.isRefinement = exports.Refinement = exports.isSuspend = exports.Suspend = exports.isUnion = exports.isMembers = exports.mapMembers = exports.Union = exports.unify = exports.flatten = exports.isTypeLiteral = exports.TypeLiteral = void 0;
const Arr = __importStar(require("./Array.js"));
const Function_js_1 = require("./Function.js");
const GlobalValue_js_1 = require("./GlobalValue.js");
const Inspectable = __importStar(require("./Inspectable.js"));
const errors_ = __importStar(require("./internal/schema/errors.js"));
const util_ = __importStar(require("./internal/schema/util.js"));
const Number = __importStar(require("./Number.js"));
const Option = __importStar(require("./Option.js"));
const Order = __importStar(require("./Order.js"));
const Predicate = __importStar(require("./Predicate.js"));
const regexp = __importStar(require("./RegExp.js"));
exports.TypeConstructorAnnotationId = Symbol.for("effect/annotation/TypeConstructor");
exports.BrandAnnotationId = Symbol.for("effect/annotation/Brand");
exports.SchemaIdAnnotationId = Symbol.for("effect/annotation/SchemaId");
exports.MessageAnnotationId = Symbol.for("effect/annotation/Message");
exports.MissingMessageAnnotationId = Symbol.for("effect/annotation/MissingMessage");
exports.IdentifierAnnotationId = Symbol.for("effect/annotation/Identifier");
exports.TitleAnnotationId = Symbol.for("effect/annotation/Title");
exports.AutoTitleAnnotationId = Symbol.for("effect/annotation/AutoTitle");
exports.DescriptionAnnotationId = Symbol.for("effect/annotation/Description");
exports.ExamplesAnnotationId = Symbol.for("effect/annotation/Examples");
exports.DefaultAnnotationId = Symbol.for("effect/annotation/Default");
exports.JSONSchemaAnnotationId = Symbol.for("effect/annotation/JSONSchema");
exports.ArbitraryAnnotationId = Symbol.for("effect/annotation/Arbitrary");
exports.PrettyAnnotationId = Symbol.for("effect/annotation/Pretty");
exports.EquivalenceAnnotationId = Symbol.for("effect/annotation/Equivalence");
exports.DocumentationAnnotationId = Symbol.for("effect/annotation/Documentation");
exports.ConcurrencyAnnotationId = Symbol.for("effect/annotation/Concurrency");
exports.BatchingAnnotationId = Symbol.for("effect/annotation/Batching");
exports.ParseIssueTitleAnnotationId = Symbol.for("effect/annotation/ParseIssueTitle");
exports.ParseOptionsAnnotationId = Symbol.for("effect/annotation/ParseOptions");
exports.DecodingFallbackAnnotationId = Symbol.for("effect/annotation/DecodingFallback");
exports.SurrogateAnnotationId = Symbol.for("effect/annotation/Surrogate");
exports.StableFilterAnnotationId = Symbol.for("effect/annotation/StableFilter");
exports.getAnnotation = (0, Function_js_1.dual)(2, (annotated, key) => Object.prototype.hasOwnProperty.call(annotated.annotations, key) ?
    Option.some(annotated.annotations[key]) :
    Option.none());
exports.getTypeConstructorAnnotation = (0, exports.getAnnotation)(exports.TypeConstructorAnnotationId);
exports.getBrandAnnotation = (0, exports.getAnnotation)(exports.BrandAnnotationId);
exports.getSchemaIdAnnotation = (0, exports.getAnnotation)(exports.SchemaIdAnnotationId);
exports.getMessageAnnotation = (0, exports.getAnnotation)(exports.MessageAnnotationId);
exports.getMissingMessageAnnotation = (0, exports.getAnnotation)(exports.MissingMessageAnnotationId);
exports.getTitleAnnotation = (0, exports.getAnnotation)(exports.TitleAnnotationId);
exports.getAutoTitleAnnotation = (0, exports.getAnnotation)(exports.AutoTitleAnnotationId);
exports.getIdentifierAnnotation = (0, exports.getAnnotation)(exports.IdentifierAnnotationId);
exports.getDescriptionAnnotation = (0, exports.getAnnotation)(exports.DescriptionAnnotationId);
exports.getExamplesAnnotation = (0, exports.getAnnotation)(exports.ExamplesAnnotationId);
exports.getDefaultAnnotation = (0, exports.getAnnotation)(exports.DefaultAnnotationId);
exports.getJSONSchemaAnnotation = (0, exports.getAnnotation)(exports.JSONSchemaAnnotationId);
exports.getDocumentationAnnotation = (0, exports.getAnnotation)(exports.DocumentationAnnotationId);
exports.getConcurrencyAnnotation = (0, exports.getAnnotation)(exports.ConcurrencyAnnotationId);
exports.getBatchingAnnotation = (0, exports.getAnnotation)(exports.BatchingAnnotationId);
exports.getParseIssueTitleAnnotation = (0, exports.getAnnotation)(exports.ParseIssueTitleAnnotationId);
exports.getParseOptionsAnnotation = (0, exports.getAnnotation)(exports.ParseOptionsAnnotationId);
exports.getDecodingFallbackAnnotation = (0, exports.getAnnotation)(exports.DecodingFallbackAnnotationId);
exports.getSurrogateAnnotation = (0, exports.getAnnotation)(exports.SurrogateAnnotationId);
const getStableFilterAnnotation = (0, exports.getAnnotation)(exports.StableFilterAnnotationId);
const hasStableFilter = (annotated) => Option.exists(getStableFilterAnnotation(annotated), (b) => b === true);
exports.hasStableFilter = hasStableFilter;
exports.JSONIdentifierAnnotationId = Symbol.for("effect/annotation/JSONIdentifier");
exports.getJSONIdentifierAnnotation = (0, exports.getAnnotation)(exports.JSONIdentifierAnnotationId);
const getJSONIdentifier = (annotated) => Option.orElse((0, exports.getJSONIdentifierAnnotation)(annotated), () => (0, exports.getIdentifierAnnotation)(annotated));
exports.getJSONIdentifier = getJSONIdentifier;
exports.ParseJsonSchemaId = Symbol.for("effect/schema/ParseJson");
class Declaration {
    typeParameters;
    decodeUnknown;
    encodeUnknown;
    annotations;
    _tag = "Declaration";
    constructor(typeParameters, decodeUnknown, encodeUnknown, annotations = {}) {
        this.typeParameters = typeParameters;
        this.decodeUnknown = decodeUnknown;
        this.encodeUnknown = encodeUnknown;
        this.annotations = annotations;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => "<declaration schema>");
    }
    toJSON() {
        return {
            _tag: this._tag,
            typeParameters: this.typeParameters.map((ast) => ast.toJSON()),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.Declaration = Declaration;
const createASTGuard = (tag) => (ast) => ast._tag === tag;
exports.isDeclaration = createASTGuard("Declaration");
class Literal {
    literal;
    annotations;
    _tag = "Literal";
    constructor(literal, annotations = {}) {
        this.literal = literal;
        this.annotations = annotations;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => Inspectable.formatUnknown(this.literal));
    }
    toJSON() {
        return {
            _tag: this._tag,
            literal: Predicate.isBigInt(this.literal) ? String(this.literal) : this.literal,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.Literal = Literal;
exports.isLiteral = createASTGuard("Literal");
const $null = new Literal(null);
exports.null = $null;
class UniqueSymbol {
    symbol;
    annotations;
    _tag = "UniqueSymbol";
    constructor(symbol, annotations = {}) {
        this.symbol = symbol;
        this.annotations = annotations;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => Inspectable.formatUnknown(this.symbol));
    }
    toJSON() {
        return {
            _tag: this._tag,
            symbol: String(this.symbol),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.UniqueSymbol = UniqueSymbol;
exports.isUniqueSymbol = createASTGuard("UniqueSymbol");
class UndefinedKeyword {
    annotations;
    _tag = "UndefinedKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.UndefinedKeyword = UndefinedKeyword;
exports.undefinedKeyword = new UndefinedKeyword({
    [exports.TitleAnnotationId]: "undefined"
});
exports.isUndefinedKeyword = createASTGuard("UndefinedKeyword");
class VoidKeyword {
    annotations;
    _tag = "VoidKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.VoidKeyword = VoidKeyword;
exports.voidKeyword = new VoidKeyword({
    [exports.TitleAnnotationId]: "void"
});
exports.isVoidKeyword = createASTGuard("VoidKeyword");
class NeverKeyword {
    annotations;
    _tag = "NeverKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.NeverKeyword = NeverKeyword;
exports.neverKeyword = new NeverKeyword({
    [exports.TitleAnnotationId]: "never"
});
exports.isNeverKeyword = createASTGuard("NeverKeyword");
class UnknownKeyword {
    annotations;
    _tag = "UnknownKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.UnknownKeyword = UnknownKeyword;
exports.unknownKeyword = new UnknownKeyword({
    [exports.TitleAnnotationId]: "unknown"
});
exports.isUnknownKeyword = createASTGuard("UnknownKeyword");
class AnyKeyword {
    annotations;
    _tag = "AnyKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.AnyKeyword = AnyKeyword;
exports.anyKeyword = new AnyKeyword({
    [exports.TitleAnnotationId]: "any"
});
exports.isAnyKeyword = createASTGuard("AnyKeyword");
class StringKeyword {
    annotations;
    _tag = "StringKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.StringKeyword = StringKeyword;
exports.stringKeyword = new StringKeyword({
    [exports.TitleAnnotationId]: "string",
    [exports.DescriptionAnnotationId]: "a string"
});
exports.isStringKeyword = createASTGuard("StringKeyword");
class NumberKeyword {
    annotations;
    _tag = "NumberKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.NumberKeyword = NumberKeyword;
exports.numberKeyword = new NumberKeyword({
    [exports.TitleAnnotationId]: "number",
    [exports.DescriptionAnnotationId]: "a number"
});
exports.isNumberKeyword = createASTGuard("NumberKeyword");
class BooleanKeyword {
    annotations;
    _tag = "BooleanKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.BooleanKeyword = BooleanKeyword;
exports.booleanKeyword = new BooleanKeyword({
    [exports.TitleAnnotationId]: "boolean",
    [exports.DescriptionAnnotationId]: "a boolean"
});
exports.isBooleanKeyword = createASTGuard("BooleanKeyword");
class BigIntKeyword {
    annotations;
    _tag = "BigIntKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.BigIntKeyword = BigIntKeyword;
exports.bigIntKeyword = new BigIntKeyword({
    [exports.TitleAnnotationId]: "bigint",
    [exports.DescriptionAnnotationId]: "a bigint"
});
exports.isBigIntKeyword = createASTGuard("BigIntKeyword");
class SymbolKeyword {
    annotations;
    _tag = "SymbolKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.SymbolKeyword = SymbolKeyword;
exports.symbolKeyword = new SymbolKeyword({
    [exports.TitleAnnotationId]: "symbol",
    [exports.DescriptionAnnotationId]: "a symbol"
});
exports.isSymbolKeyword = createASTGuard("SymbolKeyword");
class ObjectKeyword {
    annotations;
    _tag = "ObjectKeyword";
    constructor(annotations = {}) {
        this.annotations = annotations;
    }
    toString() {
        return formatKeyword(this);
    }
    toJSON() {
        return {
            _tag: this._tag,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.ObjectKeyword = ObjectKeyword;
exports.objectKeyword = new ObjectKeyword({
    [exports.TitleAnnotationId]: "object",
    [exports.DescriptionAnnotationId]: "an object in the TypeScript meaning, i.e. the `object` type"
});
exports.isObjectKeyword = createASTGuard("ObjectKeyword");
class Enums {
    enums;
    annotations;
    _tag = "Enums";
    constructor(enums, annotations = {}) {
        this.enums = enums;
        this.annotations = annotations;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => `<enum ${this.enums.length} value(s): ${this.enums.map(([_, value]) => JSON.stringify(value)).join(" | ")}>`);
    }
    toJSON() {
        return {
            _tag: this._tag,
            enums: this.enums,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.Enums = Enums;
exports.isEnums = createASTGuard("Enums");
const isTemplateLiteralSpanType = (ast) => {
    switch (ast._tag) {
        case "Literal":
        case "NumberKeyword":
        case "StringKeyword":
        case "TemplateLiteral":
            return true;
        case "Union":
            return ast.types.every(isTemplateLiteralSpanType);
    }
    return false;
};
const templateLiteralSpanUnionTypeToString = (type) => {
    switch (type._tag) {
        case "Literal":
            return JSON.stringify(String(type.literal));
        case "StringKeyword":
            return "string";
        case "NumberKeyword":
            return "number";
        case "TemplateLiteral":
            return String(type);
        case "Union":
            return type.types.map(templateLiteralSpanUnionTypeToString).join(" | ");
    }
};
const templateLiteralSpanTypeToString = (type) => {
    switch (type._tag) {
        case "Literal":
            return String(type.literal);
        case "StringKeyword":
            return "${string}";
        case "NumberKeyword":
            return "${number}";
        case "TemplateLiteral":
            return "${" + String(type) + "}";
        case "Union":
            return "${" + type.types.map(templateLiteralSpanUnionTypeToString).join(" | ") + "}";
    }
};
class TemplateLiteralSpan {
    literal;
    type;
    constructor(type, literal) {
        this.literal = literal;
        if (isTemplateLiteralSpanType(type)) {
            this.type = type;
        }
        else {
            throw new Error(errors_.getSchemaUnsupportedLiteralSpanErrorMessage(type));
        }
    }
    toString() {
        return templateLiteralSpanTypeToString(this.type) + this.literal;
    }
    toJSON() {
        return {
            type: this.type.toJSON(),
            literal: this.literal
        };
    }
}
exports.TemplateLiteralSpan = TemplateLiteralSpan;
class TemplateLiteral {
    head;
    spans;
    annotations;
    _tag = "TemplateLiteral";
    constructor(head, spans, annotations = {}) {
        this.head = head;
        this.spans = spans;
        this.annotations = annotations;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => formatTemplateLiteral(this));
    }
    toJSON() {
        return {
            _tag: this._tag,
            head: this.head,
            spans: this.spans.map((span) => span.toJSON()),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.TemplateLiteral = TemplateLiteral;
const formatTemplateLiteral = (ast) => "`" + ast.head + ast.spans.map(String).join("") +
    "`";
exports.isTemplateLiteral = createASTGuard("TemplateLiteral");
class Type {
    type;
    annotations;
    constructor(type, annotations = {}) {
        this.type = type;
        this.annotations = annotations;
    }
    toJSON() {
        return {
            type: this.type.toJSON(),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
    toString() {
        return String(this.type);
    }
}
exports.Type = Type;
class OptionalType extends Type {
    isOptional;
    constructor(type, isOptional, annotations = {}) {
        super(type, annotations);
        this.isOptional = isOptional;
    }
    toJSON() {
        return {
            type: this.type.toJSON(),
            isOptional: this.isOptional,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
    toString() {
        return String(this.type) + (this.isOptional ? "?" : "");
    }
}
exports.OptionalType = OptionalType;
const getRestASTs = (rest) => rest.map((annotatedAST) => annotatedAST.type);
class TupleType {
    elements;
    rest;
    isReadonly;
    annotations;
    _tag = "TupleType";
    constructor(elements, rest, isReadonly, annotations = {}) {
        this.elements = elements;
        this.rest = rest;
        this.isReadonly = isReadonly;
        this.annotations = annotations;
        let hasOptionalElement = false;
        let hasIllegalRequiredElement = false;
        for (const e of elements) {
            if (e.isOptional) {
                hasOptionalElement = true;
            }
            else if (hasOptionalElement) {
                hasIllegalRequiredElement = true;
                break;
            }
        }
        if (hasIllegalRequiredElement || (hasOptionalElement && rest.length > 1)) {
            throw new Error(errors_.getASTRequiredElementFollowinAnOptionalElementErrorMessage);
        }
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => formatTuple(this));
    }
    toJSON() {
        return {
            _tag: this._tag,
            elements: this.elements.map((e) => e.toJSON()),
            rest: this.rest.map((ast) => ast.toJSON()),
            isReadonly: this.isReadonly,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.TupleType = TupleType;
const formatTuple = (ast) => {
    const formattedElements = ast.elements.map(String)
        .join(", ");
    return Arr.matchLeft(ast.rest, {
        onEmpty: () => `readonly [${formattedElements}]`,
        onNonEmpty: (head, tail) => {
            const formattedHead = String(head);
            const wrappedHead = formattedHead.includes(" | ") ? `(${formattedHead})` : formattedHead;
            if (tail.length > 0) {
                const formattedTail = tail.map(String).join(", ");
                if (ast.elements.length > 0) {
                    return `readonly [${formattedElements}, ...${wrappedHead}[], ${formattedTail}]`;
                }
                else {
                    return `readonly [...${wrappedHead}[], ${formattedTail}]`;
                }
            }
            else {
                if (ast.elements.length > 0) {
                    return `readonly [${formattedElements}, ...${wrappedHead}[]]`;
                }
                else {
                    return `ReadonlyArray<${formattedHead}>`;
                }
            }
        }
    });
};
exports.isTupleType = createASTGuard("TupleType");
class PropertySignature extends OptionalType {
    name;
    isReadonly;
    constructor(name, type, isOptional, isReadonly, annotations) {
        super(type, isOptional, annotations);
        this.name = name;
        this.isReadonly = isReadonly;
    }
    toString() {
        return (this.isReadonly ? "readonly " : "") + String(this.name) + (this.isOptional ? "?" : "") + ": " +
            this.type;
    }
    toJSON() {
        return {
            name: String(this.name),
            type: this.type.toJSON(),
            isOptional: this.isOptional,
            isReadonly: this.isReadonly,
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.PropertySignature = PropertySignature;
const isParameter = (ast) => {
    switch (ast._tag) {
        case "StringKeyword":
        case "SymbolKeyword":
        case "TemplateLiteral":
            return true;
        case "Refinement":
            return (0, exports.isParameter)(ast.from);
    }
    return false;
};
exports.isParameter = isParameter;
class IndexSignature {
    type;
    isReadonly;
    parameter;
    constructor(parameter, type, isReadonly) {
        this.type = type;
        this.isReadonly = isReadonly;
        if ((0, exports.isParameter)(parameter)) {
            this.parameter = parameter;
        }
        else {
            throw new Error(errors_.getASTIndexSignatureParameterErrorMessage);
        }
    }
    toString() {
        return (this.isReadonly ? "readonly " : "") + `[x: ${this.parameter}]: ${this.type}`;
    }
    toJSON() {
        return {
            parameter: this.parameter.toJSON(),
            type: this.type.toJSON(),
            isReadonly: this.isReadonly
        };
    }
}
exports.IndexSignature = IndexSignature;
class TypeLiteral {
    annotations;
    _tag = "TypeLiteral";
    propertySignatures;
    indexSignatures;
    constructor(propertySignatures, indexSignatures, annotations = {}) {
        this.annotations = annotations;
        const keys = {};
        for (let i = 0; i < propertySignatures.length; i++) {
            const name = propertySignatures[i].name;
            if (Object.prototype.hasOwnProperty.call(keys, name)) {
                throw new Error(errors_.getASTDuplicatePropertySignatureErrorMessage(name));
            }
            keys[name] = null;
        }
        const parameters = {
            string: false,
            symbol: false
        };
        for (let i = 0; i < indexSignatures.length; i++) {
            const encodedParameter = (0, exports.getEncodedParameter)(indexSignatures[i].parameter);
            if ((0, exports.isStringKeyword)(encodedParameter)) {
                if (parameters.string) {
                    throw new Error(errors_.getASTDuplicateIndexSignatureErrorMessage("string"));
                }
                parameters.string = true;
            }
            else if ((0, exports.isSymbolKeyword)(encodedParameter)) {
                if (parameters.symbol) {
                    throw new Error(errors_.getASTDuplicateIndexSignatureErrorMessage("symbol"));
                }
                parameters.symbol = true;
            }
        }
        this.propertySignatures = propertySignatures;
        this.indexSignatures = indexSignatures;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => formatTypeLiteral(this));
    }
    toJSON() {
        return {
            _tag: this._tag,
            propertySignatures: this.propertySignatures.map((ps) => ps.toJSON()),
            indexSignatures: this.indexSignatures.map((ps) => ps.toJSON()),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.TypeLiteral = TypeLiteral;
const formatIndexSignatures = (iss) => iss.map(String).join("; ");
const formatTypeLiteral = (ast) => {
    if (ast.propertySignatures.length > 0) {
        const pss = ast.propertySignatures.map(String).join("; ");
        if (ast.indexSignatures.length > 0) {
            return `{ ${pss}; ${formatIndexSignatures(ast.indexSignatures)} }`;
        }
        else {
            return `{ ${pss} }`;
        }
    }
    else {
        if (ast.indexSignatures.length > 0) {
            return `{ ${formatIndexSignatures(ast.indexSignatures)} }`;
        }
        else {
            return "{}";
        }
    }
};
exports.isTypeLiteral = createASTGuard("TypeLiteral");
const sortCandidates = Arr.sort(Order.mapInput(Number.Order, (ast) => {
    switch (ast._tag) {
        case "AnyKeyword":
            return 0;
        case "UnknownKeyword":
            return 1;
        case "ObjectKeyword":
            return 2;
        case "StringKeyword":
        case "NumberKeyword":
        case "BooleanKeyword":
        case "BigIntKeyword":
        case "SymbolKeyword":
            return 3;
    }
    return 4;
}));
const literalMap = {
    string: "StringKeyword",
    number: "NumberKeyword",
    boolean: "BooleanKeyword",
    bigint: "BigIntKeyword"
};
const flatten = (candidates) => Arr.flatMap(candidates, (ast) => (0, exports.isUnion)(ast) ? (0, exports.flatten)(ast.types) : [ast]);
exports.flatten = flatten;
const unify = (candidates) => {
    const cs = sortCandidates(candidates);
    const out = [];
    const uniques = {};
    const literals = [];
    for (const ast of cs) {
        switch (ast._tag) {
            case "NeverKeyword":
                break;
            case "AnyKeyword":
                return [exports.anyKeyword];
            case "UnknownKeyword":
                return [exports.unknownKeyword];
            case "ObjectKeyword":
            case "UndefinedKeyword":
            case "VoidKeyword":
            case "StringKeyword":
            case "NumberKeyword":
            case "BooleanKeyword":
            case "BigIntKeyword":
            case "SymbolKeyword": {
                if (!uniques[ast._tag]) {
                    uniques[ast._tag] = ast;
                    out.push(ast);
                }
                break;
            }
            case "Literal": {
                const type = typeof ast.literal;
                switch (type) {
                    case "string":
                    case "number":
                    case "bigint":
                    case "boolean": {
                        const _tag = literalMap[type];
                        if (!uniques[_tag] && !literals.includes(ast.literal)) {
                            literals.push(ast.literal);
                            out.push(ast);
                        }
                        break;
                    }
                    case "object": {
                        if (!literals.includes(ast.literal)) {
                            literals.push(ast.literal);
                            out.push(ast);
                        }
                        break;
                    }
                }
                break;
            }
            case "UniqueSymbol": {
                if (!uniques["SymbolKeyword"] && !literals.includes(ast.symbol)) {
                    literals.push(ast.symbol);
                    out.push(ast);
                }
                break;
            }
            case "TupleType": {
                if (!uniques["ObjectKeyword"]) {
                    out.push(ast);
                }
                break;
            }
            case "TypeLiteral": {
                if (ast.propertySignatures.length === 0 && ast.indexSignatures.length === 0) {
                    if (!uniques["{}"]) {
                        uniques["{}"] = ast;
                        out.push(ast);
                    }
                }
                else if (!uniques["ObjectKeyword"]) {
                    out.push(ast);
                }
                break;
            }
            default:
                out.push(ast);
        }
    }
    return out;
};
exports.unify = unify;
class Union {
    types;
    annotations;
    static make = (types, annotations) => {
        return (0, exports.isMembers)(types) ? new Union(types, annotations) : types.length === 1 ? types[0] : exports.neverKeyword;
    };
    static unify = (candidates, annotations) => {
        return Union.make((0, exports.unify)((0, exports.flatten)(candidates)), annotations);
    };
    _tag = "Union";
    constructor(types, annotations = {}) {
        this.types = types;
        this.annotations = annotations;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => this.types.map(String).join(" | "));
    }
    toJSON() {
        return {
            _tag: this._tag,
            types: this.types.map((ast) => ast.toJSON()),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.Union = Union;
const mapMembers = (members, f) => members.map(f);
exports.mapMembers = mapMembers;
const isMembers = (as) => as.length > 1;
exports.isMembers = isMembers;
exports.isUnion = createASTGuard("Union");
const toJSONMemoMap = (0, GlobalValue_js_1.globalValue)(Symbol.for("effect/Schema/AST/toJSONMemoMap"), () => new WeakMap());
class Suspend {
    f;
    annotations;
    _tag = "Suspend";
    constructor(f, annotations = {}) {
        this.f = f;
        this.annotations = annotations;
        this.f = util_.memoizeThunk(f);
    }
    toString() {
        return getExpected(this).pipe(Option.orElse(() => Option.flatMap(Option.liftThrowable(this.f)(), (ast) => getExpected(ast))), Option.getOrElse(() => "<suspended schema>"));
    }
    toJSON() {
        const ast = this.f();
        let out = toJSONMemoMap.get(ast);
        if (out) {
            return out;
        }
        toJSONMemoMap.set(ast, { _tag: this._tag });
        out = {
            _tag: this._tag,
            ast: ast.toJSON(),
            annotations: toJSONAnnotations(this.annotations)
        };
        toJSONMemoMap.set(ast, out);
        return out;
    }
}
exports.Suspend = Suspend;
exports.isSuspend = createASTGuard("Suspend");
class Refinement {
    from;
    filter;
    annotations;
    _tag = "Refinement";
    constructor(from, filter, annotations = {}) {
        this.from = from;
        this.filter = filter;
        this.annotations = annotations;
    }
    toString() {
        return (0, exports.getIdentifierAnnotation)(this).pipe(Option.getOrElse(() => Option.match(getOrElseExpected(this), {
            onNone: () => `{ ${this.from} | filter }`,
            onSome: (expected) => (0, exports.isRefinement)(this.from) ? String(this.from) + " & " + expected : expected
        })));
    }
    toJSON() {
        return {
            _tag: this._tag,
            from: this.from.toJSON(),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.Refinement = Refinement;
exports.isRefinement = createASTGuard("Refinement");
exports.defaultParseOption = {};
class Transformation {
    from;
    to;
    transformation;
    annotations;
    _tag = "Transformation";
    constructor(from, to, transformation, annotations = {}) {
        this.from = from;
        this.to = to;
        this.transformation = transformation;
        this.annotations = annotations;
    }
    toString() {
        return Option.getOrElse(getExpected(this), () => `(${String(this.from)} <-> ${String(this.to)})`);
    }
    toJSON() {
        return {
            _tag: this._tag,
            from: this.from.toJSON(),
            to: this.to.toJSON(),
            annotations: toJSONAnnotations(this.annotations)
        };
    }
}
exports.Transformation = Transformation;
exports.isTransformation = createASTGuard("Transformation");
class FinalTransformation {
    decode;
    encode;
    _tag = "FinalTransformation";
    constructor(decode, encode) {
        this.decode = decode;
        this.encode = encode;
    }
}
exports.FinalTransformation = FinalTransformation;
const createTransformationGuard = (tag) => (ast) => ast._tag === tag;
exports.isFinalTransformation = createTransformationGuard("FinalTransformation");
class ComposeTransformation {
    _tag = "ComposeTransformation";
}
exports.ComposeTransformation = ComposeTransformation;
exports.composeTransformation = new ComposeTransformation();
exports.isComposeTransformation = createTransformationGuard("ComposeTransformation");
class PropertySignatureTransformation {
    from;
    to;
    decode;
    encode;
    constructor(from, to, decode, encode) {
        this.from = from;
        this.to = to;
        this.decode = decode;
        this.encode = encode;
    }
}
exports.PropertySignatureTransformation = PropertySignatureTransformation;
const isRenamingPropertySignatureTransformation = (t) => t.decode === Function_js_1.identity && t.encode === Function_js_1.identity;
class TypeLiteralTransformation {
    propertySignatureTransformations;
    _tag = "TypeLiteralTransformation";
    constructor(propertySignatureTransformations) {
        this.propertySignatureTransformations = propertySignatureTransformations;
        const fromKeys = {};
        const toKeys = {};
        for (const pst of propertySignatureTransformations) {
            const from = pst.from;
            if (fromKeys[from]) {
                throw new Error(errors_.getASTDuplicatePropertySignatureTransformationErrorMessage(from));
            }
            fromKeys[from] = true;
            const to = pst.to;
            if (toKeys[to]) {
                throw new Error(errors_.getASTDuplicatePropertySignatureTransformationErrorMessage(to));
            }
            toKeys[to] = true;
        }
    }
}
exports.TypeLiteralTransformation = TypeLiteralTransformation;
exports.isTypeLiteralTransformation = createTransformationGuard("TypeLiteralTransformation");
const annotations = (ast, overrides) => {
    const d = Object.getOwnPropertyDescriptors(ast);
    const base = { ...ast.annotations };
    delete base[exports.IdentifierAnnotationId];
    const value = { ...base, ...overrides };
    const surrogate = (0, exports.getSurrogateAnnotation)(ast);
    if (Option.isSome(surrogate)) {
        value[exports.SurrogateAnnotationId] = (0, exports.annotations)(surrogate.value, overrides);
    }
    d.annotations.value = value;
    return Object.create(Object.getPrototypeOf(ast), d);
};
exports.annotations = annotations;
const keyof = (ast) => Union.unify(_keyof(ast));
exports.keyof = keyof;
const STRING_KEYWORD_PATTERN = "[\\s\\S]*?";
const NUMBER_KEYWORD_PATTERN = "[+-]?\\d*\\.?\\d+(?:[Ee][+-]?\\d+)?";
const getTemplateLiteralSpanTypePattern = (type, capture) => {
    switch (type._tag) {
        case "Literal":
            return regexp.escape(String(type.literal));
        case "StringKeyword":
            return STRING_KEYWORD_PATTERN;
        case "NumberKeyword":
            return NUMBER_KEYWORD_PATTERN;
        case "TemplateLiteral":
            return getTemplateLiteralPattern(type, capture, false);
        case "Union":
            return type.types.map((type) => getTemplateLiteralSpanTypePattern(type, capture)).join("|");
    }
};
const handleTemplateLiteralSpanTypeParens = (type, s, capture, top) => {
    if ((0, exports.isUnion)(type)) {
        if (capture && !top) {
            return `(?:${s})`;
        }
    }
    else if (!capture || !top) {
        return s;
    }
    return `(${s})`;
};
const getTemplateLiteralPattern = (ast, capture, top) => {
    let pattern = ``;
    if (ast.head !== "") {
        const head = regexp.escape(ast.head);
        pattern += capture && top ? `(${head})` : head;
    }
    for (const span of ast.spans) {
        const spanPattern = getTemplateLiteralSpanTypePattern(span.type, capture);
        pattern += handleTemplateLiteralSpanTypeParens(span.type, spanPattern, capture, top);
        if (span.literal !== "") {
            const literal = regexp.escape(span.literal);
            pattern += capture && top ? `(${literal})` : literal;
        }
    }
    return pattern;
};
const getTemplateLiteralRegExp = (ast) => new RegExp(`^${getTemplateLiteralPattern(ast, false, true)}$`);
exports.getTemplateLiteralRegExp = getTemplateLiteralRegExp;
const getTemplateLiteralCapturingRegExp = (ast) => new RegExp(`^${getTemplateLiteralPattern(ast, true, true)}$`);
exports.getTemplateLiteralCapturingRegExp = getTemplateLiteralCapturingRegExp;
const getPropertySignatures = (ast) => {
    const annotation = (0, exports.getSurrogateAnnotation)(ast);
    if (Option.isSome(annotation)) {
        return (0, exports.getPropertySignatures)(annotation.value);
    }
    switch (ast._tag) {
        case "TypeLiteral":
            return ast.propertySignatures.slice();
        case "Suspend":
            return (0, exports.getPropertySignatures)(ast.f());
        case "Refinement":
            return (0, exports.getPropertySignatures)(ast.from);
    }
    return getPropertyKeys(ast).map((name) => (0, exports.getPropertyKeyIndexedAccess)(ast, name));
};
exports.getPropertySignatures = getPropertySignatures;
const getIndexSignatures = (ast) => {
    const annotation = (0, exports.getSurrogateAnnotation)(ast);
    if (Option.isSome(annotation)) {
        return getIndexSignatures(annotation.value);
    }
    switch (ast._tag) {
        case "TypeLiteral":
            return ast.indexSignatures.slice();
        case "Suspend":
            return getIndexSignatures(ast.f());
        case "Refinement":
            return getIndexSignatures(ast.from);
        case "Transformation":
            return getIndexSignatures(ast.to);
    }
    return [];
};
const getNumberIndexedAccess = (ast) => {
    switch (ast._tag) {
        case "TupleType": {
            let hasOptional = false;
            let out = [];
            for (const e of ast.elements) {
                if (e.isOptional) {
                    hasOptional = true;
                }
                out.push(e.type);
            }
            if (hasOptional) {
                out.push(exports.undefinedKeyword);
            }
            out = out.concat(getRestASTs(ast.rest));
            return Union.make(out);
        }
        case "Refinement":
            return (0, exports.getNumberIndexedAccess)(ast.from);
        case "Union":
            return Union.make(ast.types.map(exports.getNumberIndexedAccess));
        case "Suspend":
            return (0, exports.getNumberIndexedAccess)(ast.f());
    }
    throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
};
exports.getNumberIndexedAccess = getNumberIndexedAccess;
const getTypeLiteralPropertySignature = (ast, name) => {
    const ops = Arr.findFirst(ast.propertySignatures, (ps) => ps.name === name);
    if (Option.isSome(ops)) {
        return ops.value;
    }
    if (Predicate.isString(name)) {
        let out = undefined;
        for (const is of ast.indexSignatures) {
            const encodedParameter = (0, exports.getEncodedParameter)(is.parameter);
            switch (encodedParameter._tag) {
                case "TemplateLiteral": {
                    const regex = (0, exports.getTemplateLiteralRegExp)(encodedParameter);
                    if (regex.test(name)) {
                        return new PropertySignature(name, is.type, false, true);
                    }
                    break;
                }
                case "StringKeyword": {
                    if (out === undefined) {
                        out = new PropertySignature(name, is.type, false, true);
                    }
                }
            }
        }
        if (out) {
            return out;
        }
    }
    else if (Predicate.isSymbol(name)) {
        for (const is of ast.indexSignatures) {
            const encodedParameter = (0, exports.getEncodedParameter)(is.parameter);
            if ((0, exports.isSymbolKeyword)(encodedParameter)) {
                return new PropertySignature(name, is.type, false, true);
            }
        }
    }
};
const getPropertyKeyIndexedAccess = (ast, name) => {
    const annotation = (0, exports.getSurrogateAnnotation)(ast);
    if (Option.isSome(annotation)) {
        return (0, exports.getPropertyKeyIndexedAccess)(annotation.value, name);
    }
    switch (ast._tag) {
        case "TypeLiteral": {
            const ps = getTypeLiteralPropertySignature(ast, name);
            if (ps) {
                return ps;
            }
            break;
        }
        case "Union":
            return new PropertySignature(name, Union.make(ast.types.map((ast) => (0, exports.getPropertyKeyIndexedAccess)(ast, name).type)), false, true);
        case "Suspend":
            return (0, exports.getPropertyKeyIndexedAccess)(ast.f(), name);
        case "Refinement":
            return (0, exports.getPropertyKeyIndexedAccess)(ast.from, name);
        case "Transformation":
            return (0, exports.getPropertyKeyIndexedAccess)(ast.to, name);
    }
    throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
};
exports.getPropertyKeyIndexedAccess = getPropertyKeyIndexedAccess;
const getPropertyKeys = (ast) => {
    const annotation = (0, exports.getSurrogateAnnotation)(ast);
    if (Option.isSome(annotation)) {
        return getPropertyKeys(annotation.value);
    }
    switch (ast._tag) {
        case "TypeLiteral":
            return ast.propertySignatures.map((ps) => ps.name);
        case "Union":
            return ast.types.slice(1).reduce((out, ast) => Arr.intersection(out, getPropertyKeys(ast)), getPropertyKeys(ast.types[0]));
        case "Suspend":
            return getPropertyKeys(ast.f());
        case "Refinement":
            return getPropertyKeys(ast.from);
        case "Transformation":
            return getPropertyKeys(ast.to);
    }
    return [];
};
const record = (key, value) => {
    const propertySignatures = [];
    const indexSignatures = [];
    const go = (key) => {
        switch (key._tag) {
            case "NeverKeyword":
                break;
            case "StringKeyword":
            case "SymbolKeyword":
            case "TemplateLiteral":
            case "Refinement":
                indexSignatures.push(new IndexSignature(key, value, true));
                break;
            case "Literal":
                if (Predicate.isString(key.literal) || Predicate.isNumber(key.literal)) {
                    propertySignatures.push(new PropertySignature(key.literal, value, false, true));
                }
                else {
                    throw new Error(errors_.getASTUnsupportedLiteralErrorMessage(key.literal));
                }
                break;
            case "Enums": {
                for (const [_, name] of key.enums) {
                    propertySignatures.push(new PropertySignature(name, value, false, true));
                }
                break;
            }
            case "UniqueSymbol":
                propertySignatures.push(new PropertySignature(key.symbol, value, false, true));
                break;
            case "Union":
                key.types.forEach(go);
                break;
            default:
                throw new Error(errors_.getASTUnsupportedKeySchemaErrorMessage(key));
        }
    };
    go(key);
    return { propertySignatures, indexSignatures };
};
exports.record = record;
const pick = (ast, keys) => {
    const annotation = (0, exports.getSurrogateAnnotation)(ast);
    if (Option.isSome(annotation)) {
        return (0, exports.pick)(annotation.value, keys);
    }
    switch (ast._tag) {
        case "TypeLiteral": {
            const pss = [];
            const names = {};
            for (const ps of ast.propertySignatures) {
                names[ps.name] = null;
                if (keys.includes(ps.name)) {
                    pss.push(ps);
                }
            }
            for (const key of keys) {
                if (!(key in names)) {
                    const ps = getTypeLiteralPropertySignature(ast, key);
                    if (ps) {
                        pss.push(ps);
                    }
                }
            }
            return new TypeLiteral(pss, []);
        }
        case "Union":
            return new TypeLiteral(keys.map((name) => (0, exports.getPropertyKeyIndexedAccess)(ast, name)), []);
        case "Suspend":
            return (0, exports.pick)(ast.f(), keys);
        case "Refinement":
            return (0, exports.pick)(ast.from, keys);
        case "Transformation": {
            switch (ast.transformation._tag) {
                case "ComposeTransformation":
                    return new Transformation((0, exports.pick)(ast.from, keys), (0, exports.pick)(ast.to, keys), exports.composeTransformation);
                case "TypeLiteralTransformation": {
                    const ts = [];
                    const fromKeys = [];
                    for (const k of keys) {
                        const t = ast.transformation.propertySignatureTransformations.find((t) => t.to === k);
                        if (t) {
                            ts.push(t);
                            fromKeys.push(t.from);
                        }
                        else {
                            fromKeys.push(k);
                        }
                    }
                    return Arr.isNonEmptyReadonlyArray(ts) ?
                        new Transformation((0, exports.pick)(ast.from, fromKeys), (0, exports.pick)(ast.to, keys), new TypeLiteralTransformation(ts)) :
                        (0, exports.pick)(ast.from, fromKeys);
                }
            }
        }
    }
    throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
};
exports.pick = pick;
const omit = (ast, keys) => {
    let indexSignatures = getIndexSignatures(ast);
    if (indexSignatures.length > 0) {
        if (indexSignatures.some((is) => (0, exports.isStringKeyword)((0, exports.getEncodedParameter)(is.parameter)))) {
            indexSignatures = indexSignatures.filter((is) => !(0, exports.isTemplateLiteral)((0, exports.getEncodedParameter)(is.parameter)));
        }
        return new TypeLiteral([], indexSignatures);
    }
    return (0, exports.pick)(ast, getPropertyKeys(ast).filter((name) => !keys.includes(name)));
};
exports.omit = omit;
const orUndefined = (ast) => Union.make([ast, exports.undefinedKeyword]);
exports.orUndefined = orUndefined;
const partial = (ast, options) => {
    const exact = options?.exact === true;
    switch (ast._tag) {
        case "TupleType":
            return new TupleType(ast.elements.map((e) => new OptionalType(exact ? e.type : (0, exports.orUndefined)(e.type), true)), Arr.match(ast.rest, {
                onEmpty: () => ast.rest,
                onNonEmpty: (rest) => [new Type(Union.make([...getRestASTs(rest), exports.undefinedKeyword]))]
            }), ast.isReadonly);
        case "TypeLiteral":
            return new TypeLiteral(ast.propertySignatures.map((ps) => new PropertySignature(ps.name, exact ? ps.type : (0, exports.orUndefined)(ps.type), true, ps.isReadonly, ps.annotations)), ast.indexSignatures.map((is) => new IndexSignature(is.parameter, (0, exports.orUndefined)(is.type), is.isReadonly)));
        case "Union":
            return Union.make(ast.types.map((member) => (0, exports.partial)(member, options)));
        case "Suspend":
            return new Suspend(() => (0, exports.partial)(ast.f(), options));
        case "Declaration":
        case "Refinement":
            throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
        case "Transformation": {
            if ((0, exports.isTypeLiteralTransformation)(ast.transformation) &&
                ast.transformation.propertySignatureTransformations.every(isRenamingPropertySignatureTransformation)) {
                return new Transformation((0, exports.partial)(ast.from, options), (0, exports.partial)(ast.to, options), ast.transformation);
            }
            throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
        }
    }
    return ast;
};
exports.partial = partial;
const required = (ast) => {
    switch (ast._tag) {
        case "TupleType":
            return new TupleType(ast.elements.map((e) => new OptionalType(e.type, false)), ast.rest, ast.isReadonly);
        case "TypeLiteral":
            return new TypeLiteral(ast.propertySignatures.map((f) => new PropertySignature(f.name, f.type, false, f.isReadonly, f.annotations)), ast.indexSignatures);
        case "Union":
            return Union.make(ast.types.map((member) => (0, exports.required)(member)));
        case "Suspend":
            return new Suspend(() => (0, exports.required)(ast.f()));
        case "Declaration":
        case "Refinement":
            throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
        case "Transformation": {
            if ((0, exports.isTypeLiteralTransformation)(ast.transformation) &&
                ast.transformation.propertySignatureTransformations.every(isRenamingPropertySignatureTransformation)) {
                return new Transformation((0, exports.required)(ast.from), (0, exports.required)(ast.to), ast.transformation);
            }
            throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
        }
    }
    return ast;
};
exports.required = required;
const mutable = (ast) => {
    switch (ast._tag) {
        case "TupleType":
            return ast.isReadonly === false ? ast : new TupleType(ast.elements, ast.rest, false, ast.annotations);
        case "TypeLiteral": {
            const propertySignatures = changeMap(ast.propertySignatures, (ps) => ps.isReadonly === false ? ps : new PropertySignature(ps.name, ps.type, ps.isOptional, false, ps.annotations));
            const indexSignatures = changeMap(ast.indexSignatures, (is) => is.isReadonly === false ? is : new IndexSignature(is.parameter, is.type, false));
            return propertySignatures === ast.propertySignatures && indexSignatures === ast.indexSignatures ?
                ast :
                new TypeLiteral(propertySignatures, indexSignatures, ast.annotations);
        }
        case "Union": {
            const types = changeMap(ast.types, exports.mutable);
            return types === ast.types ? ast : Union.make(types, ast.annotations);
        }
        case "Suspend":
            return new Suspend(() => (0, exports.mutable)(ast.f()), ast.annotations);
        case "Refinement": {
            const from = (0, exports.mutable)(ast.from);
            return from === ast.from ? ast : new Refinement(from, ast.filter, ast.annotations);
        }
        case "Transformation": {
            const from = (0, exports.mutable)(ast.from);
            const to = (0, exports.mutable)(ast.to);
            return from === ast.from && to === ast.to ?
                ast :
                new Transformation(from, to, ast.transformation, ast.annotations);
        }
    }
    return ast;
};
exports.mutable = mutable;
const getCompiler = (match) => {
    const compile = (ast, path) => match[ast._tag](ast, compile, path);
    return compile;
};
exports.getCompiler = getCompiler;
const pickAnnotations = (annotationIds) => (annotated) => {
    let out = undefined;
    for (const id of annotationIds) {
        if (Object.prototype.hasOwnProperty.call(annotated.annotations, id)) {
            if (out === undefined) {
                out = {};
            }
            out[id] = annotated.annotations[id];
        }
    }
    return out;
};
exports.pickAnnotations = pickAnnotations;
const omitAnnotations = (annotationIds) => (annotated) => {
    const out = { ...annotated.annotations };
    for (const id of annotationIds) {
        delete out[id];
    }
    return out;
};
exports.omitAnnotations = omitAnnotations;
const preserveTransformationAnnotations = (0, exports.pickAnnotations)([
    exports.ExamplesAnnotationId,
    exports.DefaultAnnotationId,
    exports.JSONSchemaAnnotationId,
    exports.ArbitraryAnnotationId,
    exports.PrettyAnnotationId,
    exports.EquivalenceAnnotationId
]);
const typeAST = (ast) => {
    switch (ast._tag) {
        case "Declaration": {
            const typeParameters = changeMap(ast.typeParameters, exports.typeAST);
            return typeParameters === ast.typeParameters ?
                ast :
                new Declaration(typeParameters, ast.decodeUnknown, ast.encodeUnknown, ast.annotations);
        }
        case "TupleType": {
            const elements = changeMap(ast.elements, (e) => {
                const type = (0, exports.typeAST)(e.type);
                return type === e.type ? e : new OptionalType(type, e.isOptional);
            });
            const restASTs = getRestASTs(ast.rest);
            const rest = changeMap(restASTs, exports.typeAST);
            return elements === ast.elements && rest === restASTs ?
                ast :
                new TupleType(elements, rest.map((type) => new Type(type)), ast.isReadonly, ast.annotations);
        }
        case "TypeLiteral": {
            const propertySignatures = changeMap(ast.propertySignatures, (p) => {
                const type = (0, exports.typeAST)(p.type);
                return type === p.type ? p : new PropertySignature(p.name, type, p.isOptional, p.isReadonly);
            });
            const indexSignatures = changeMap(ast.indexSignatures, (is) => {
                const type = (0, exports.typeAST)(is.type);
                return type === is.type ? is : new IndexSignature(is.parameter, type, is.isReadonly);
            });
            return propertySignatures === ast.propertySignatures && indexSignatures === ast.indexSignatures ?
                ast :
                new TypeLiteral(propertySignatures, indexSignatures, ast.annotations);
        }
        case "Union": {
            const types = changeMap(ast.types, exports.typeAST);
            return types === ast.types ? ast : Union.make(types, ast.annotations);
        }
        case "Suspend":
            return new Suspend(() => (0, exports.typeAST)(ast.f()), ast.annotations);
        case "Refinement": {
            const from = (0, exports.typeAST)(ast.from);
            return from === ast.from ?
                ast :
                new Refinement(from, ast.filter, ast.annotations);
        }
        case "Transformation": {
            const preserve = preserveTransformationAnnotations(ast);
            return (0, exports.typeAST)(preserve !== undefined ?
                (0, exports.annotations)(ast.to, preserve) :
                ast.to);
        }
    }
    return ast;
};
exports.typeAST = typeAST;
function changeMap(as, f) {
    let changed = false;
    const out = Arr.allocate(as.length);
    for (let i = 0; i < as.length; i++) {
        const a = as[i];
        const fa = f(a);
        if (fa !== a) {
            changed = true;
        }
        out[i] = fa;
    }
    return changed ? out : as;
}
const getTransformationFrom = (ast) => {
    switch (ast._tag) {
        case "Transformation":
            return ast.from;
        case "Refinement":
            return (0, exports.getTransformationFrom)(ast.from);
        case "Suspend":
            return (0, exports.getTransformationFrom)(ast.f());
    }
};
exports.getTransformationFrom = getTransformationFrom;
const encodedAST_ = (ast, isBound) => {
    switch (ast._tag) {
        case "Declaration": {
            const typeParameters = changeMap(ast.typeParameters, (ast) => encodedAST_(ast, isBound));
            return typeParameters === ast.typeParameters ?
                ast :
                new Declaration(typeParameters, ast.decodeUnknown, ast.encodeUnknown);
        }
        case "TupleType": {
            const elements = changeMap(ast.elements, (e) => {
                const type = encodedAST_(e.type, isBound);
                return type === e.type ? e : new OptionalType(type, e.isOptional);
            });
            const restASTs = getRestASTs(ast.rest);
            const rest = changeMap(restASTs, (ast) => encodedAST_(ast, isBound));
            return elements === ast.elements && rest === restASTs ?
                ast :
                new TupleType(elements, rest.map((ast) => new Type(ast)), ast.isReadonly);
        }
        case "TypeLiteral": {
            const propertySignatures = changeMap(ast.propertySignatures, (ps) => {
                const type = encodedAST_(ps.type, isBound);
                return type === ps.type
                    ? ps
                    : new PropertySignature(ps.name, type, ps.isOptional, ps.isReadonly);
            });
            const indexSignatures = changeMap(ast.indexSignatures, (is) => {
                const type = encodedAST_(is.type, isBound);
                return type === is.type ? is : new IndexSignature(is.parameter, type, is.isReadonly);
            });
            return propertySignatures === ast.propertySignatures && indexSignatures === ast.indexSignatures ?
                ast :
                new TypeLiteral(propertySignatures, indexSignatures);
        }
        case "Union": {
            const types = changeMap(ast.types, (ast) => encodedAST_(ast, isBound));
            return types === ast.types ? ast : Union.make(types);
        }
        case "Suspend": {
            let borrowedAnnotations = undefined;
            const identifier = (0, exports.getJSONIdentifier)(ast);
            if (Option.isSome(identifier)) {
                const suffix = isBound ? "Bound" : "";
                borrowedAnnotations = { [exports.JSONIdentifierAnnotationId]: `${identifier.value}Encoded${suffix}` };
            }
            return new Suspend(() => encodedAST_(ast.f(), isBound), borrowedAnnotations);
        }
        case "Refinement": {
            const from = encodedAST_(ast.from, isBound);
            if (isBound) {
                if (from === ast.from)
                    return ast;
                if ((0, exports.getTransformationFrom)(ast.from) === undefined && (0, exports.hasStableFilter)(ast)) {
                    return new Refinement(from, ast.filter, ast.annotations);
                }
                return from;
            }
            else {
                return from;
            }
        }
        case "Transformation":
            return encodedAST_(ast.from, isBound);
    }
    return ast;
};
const encodedAST = (ast) => encodedAST_(ast, false);
exports.encodedAST = encodedAST;
const encodedBoundAST = (ast) => encodedAST_(ast, true);
exports.encodedBoundAST = encodedBoundAST;
const toJSONAnnotations = (annotations) => {
    const out = {};
    for (const k of Object.getOwnPropertySymbols(annotations)) {
        out[String(k)] = annotations[k];
    }
    return out;
};
const getEncodedParameter = (ast) => {
    switch (ast._tag) {
        case "StringKeyword":
        case "SymbolKeyword":
        case "TemplateLiteral":
            return ast;
        case "Refinement":
            return (0, exports.getEncodedParameter)(ast.from);
    }
};
exports.getEncodedParameter = getEncodedParameter;
const equals = (self, that) => {
    switch (self._tag) {
        case "Literal":
            return (0, exports.isLiteral)(that) && that.literal === self.literal;
        case "UniqueSymbol":
            return (0, exports.isUniqueSymbol)(that) && that.symbol === self.symbol;
        case "UndefinedKeyword":
        case "VoidKeyword":
        case "NeverKeyword":
        case "UnknownKeyword":
        case "AnyKeyword":
        case "StringKeyword":
        case "NumberKeyword":
        case "BooleanKeyword":
        case "BigIntKeyword":
        case "SymbolKeyword":
        case "ObjectKeyword":
            return that._tag === self._tag;
        case "TemplateLiteral":
            return (0, exports.isTemplateLiteral)(that) && that.head === self.head && equalsTemplateLiteralSpan(that.spans, self.spans);
        case "Enums":
            return (0, exports.isEnums)(that) && equalsEnums(that.enums, self.enums);
        case "Union":
            return (0, exports.isUnion)(that) && equalsUnion(self.types, that.types);
        case "Refinement":
        case "TupleType":
        case "TypeLiteral":
        case "Suspend":
        case "Transformation":
        case "Declaration":
            return self === that;
    }
};
exports.equals = equals;
const equalsTemplateLiteralSpan = Arr.getEquivalence((self, that) => {
    return self.literal === that.literal && (0, exports.equals)(self.type, that.type);
});
const equalsEnums = Arr.getEquivalence((self, that) => that[0] === self[0] && that[1] === self[1]);
const equalsUnion = Arr.getEquivalence(exports.equals);
const intersection = Arr.intersectionWith(exports.equals);
const _keyof = (ast) => {
    switch (ast._tag) {
        case "Declaration": {
            const annotation = (0, exports.getSurrogateAnnotation)(ast);
            if (Option.isSome(annotation)) {
                return _keyof(annotation.value);
            }
            break;
        }
        case "TypeLiteral":
            return ast.propertySignatures.map((p) => Predicate.isSymbol(p.name) ? new UniqueSymbol(p.name) : new Literal(p.name)).concat(ast.indexSignatures.map((is) => (0, exports.getEncodedParameter)(is.parameter)));
        case "Suspend":
            return _keyof(ast.f());
        case "Union":
            return ast.types.slice(1).reduce((out, ast) => intersection(out, _keyof(ast)), _keyof(ast.types[0]));
        case "Transformation":
            return _keyof(ast.to);
    }
    throw new Error(errors_.getASTUnsupportedSchemaErrorMessage(ast));
};
const compose = (ab, cd) => new Transformation(ab, cd, exports.composeTransformation);
exports.compose = compose;
const rename = (ast, mapping) => {
    switch (ast._tag) {
        case "TypeLiteral": {
            const propertySignatureTransformations = [];
            for (const key of Reflect.ownKeys(mapping)) {
                const name = mapping[key];
                if (name !== undefined) {
                    propertySignatureTransformations.push(new PropertySignatureTransformation(key, name, Function_js_1.identity, Function_js_1.identity));
                }
            }
            if (propertySignatureTransformations.length === 0) {
                return ast;
            }
            return new Transformation(ast, new TypeLiteral(ast.propertySignatures.map((ps) => {
                const name = mapping[ps.name];
                return new PropertySignature(name === undefined ? ps.name : name, (0, exports.typeAST)(ps.type), ps.isOptional, ps.isReadonly, ps.annotations);
            }), ast.indexSignatures), new TypeLiteralTransformation(propertySignatureTransformations));
        }
        case "Union":
            return Union.make(ast.types.map((ast) => (0, exports.rename)(ast, mapping)));
        case "Suspend":
            return new Suspend(() => (0, exports.rename)(ast.f(), mapping));
        case "Transformation":
            return (0, exports.compose)(ast, (0, exports.rename)((0, exports.typeAST)(ast), mapping));
    }
    throw new Error(errors_.getASTUnsupportedRenameSchemaErrorMessage(ast));
};
exports.rename = rename;
const formatKeyword = (ast) => Option.getOrElse(getExpected(ast), () => ast._tag);
function getBrands(ast) {
    return Option.match((0, exports.getBrandAnnotation)(ast), {
        onNone: () => "",
        onSome: (brands) => brands.map((brand) => ` & Brand<${Inspectable.formatUnknown(brand)}>`).join("")
    });
}
const getOrElseExpected = (ast) => (0, exports.getTitleAnnotation)(ast).pipe(Option.orElse(() => (0, exports.getDescriptionAnnotation)(ast)), Option.orElse(() => (0, exports.getAutoTitleAnnotation)(ast)), Option.map((s) => s + getBrands(ast)));
const getExpected = (ast) => Option.orElse((0, exports.getIdentifierAnnotation)(ast), () => getOrElseExpected(ast));
const pruneUndefined = (ast, self, onTransformation) => {
    switch (ast._tag) {
        case "UndefinedKeyword":
            return exports.neverKeyword;
        case "Union": {
            const types = [];
            let hasUndefined = false;
            for (const type of ast.types) {
                const pruned = self(type);
                if (pruned) {
                    hasUndefined = true;
                    if (!(0, exports.isNeverKeyword)(pruned)) {
                        types.push(pruned);
                    }
                }
                else {
                    types.push(type);
                }
            }
            if (hasUndefined) {
                return Union.make(types);
            }
            break;
        }
        case "Suspend":
            return self(ast.f());
        case "Transformation":
            return onTransformation(ast);
    }
};
exports.pruneUndefined = pruneUndefined;
//# sourceMappingURL=SchemaAST.js.map
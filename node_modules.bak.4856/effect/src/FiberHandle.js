"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.awaitEmpty = exports.join = exports.runtimePromise = exports.runtime = exports.run = exports.clear = exports.get = exports.unsafeGet = exports.set = exports.unsafeSet = exports.makeRuntimePromise = exports.makeRuntime = exports.make = exports.isFiberHandle = exports.TypeId = void 0;
const Cause = __importStar(require("./Cause.js"));
const Deferred = __importStar(require("./Deferred.js"));
const Effect = __importStar(require("./Effect.js"));
const Exit = __importStar(require("./Exit.js"));
const Fiber = __importStar(require("./Fiber.js"));
const FiberId = __importStar(require("./FiberId.js"));
const Function_js_1 = require("./Function.js");
const HashSet = __importStar(require("./HashSet.js"));
const Inspectable = __importStar(require("./Inspectable.js"));
const Option = __importStar(require("./Option.js"));
const Pipeable_js_1 = require("./Pipeable.js");
const Predicate = __importStar(require("./Predicate.js"));
const Runtime = __importStar(require("./Runtime.js"));
exports.TypeId = Symbol.for("effect/FiberHandle");
const isFiberHandle = (u) => Predicate.hasProperty(u, exports.TypeId);
exports.isFiberHandle = isFiberHandle;
const Proto = {
    [exports.TypeId]: exports.TypeId,
    toString() {
        return Inspectable.format(this.toJSON());
    },
    toJSON() {
        return {
            _id: "FiberHandle",
            state: this.state
        };
    },
    [Inspectable.NodeInspectSymbol]() {
        return this.toJSON();
    },
    pipe() {
        return (0, Pipeable_js_1.pipeArguments)(this, arguments);
    }
};
const unsafeMake = (deferred) => {
    const self = Object.create(Proto);
    self.state = { _tag: "Open", fiber: undefined };
    self.deferred = deferred;
    return self;
};
const make = () => Effect.acquireRelease(Effect.map(Deferred.make(), (deferred) => unsafeMake(deferred)), (handle) => Effect.withFiberRuntime((parent) => {
    const state = handle.state;
    if (state._tag === "Closed")
        return Effect.void;
    handle.state = { _tag: "Closed" };
    return state.fiber ?
        Effect.intoDeferred(Effect.asVoid(Fiber.interruptAs(state.fiber, FiberId.combine(parent.id(), internalFiberId))), handle.deferred) :
        Deferred.done(handle.deferred, Exit.void);
}));
exports.make = make;
const makeRuntime = () => Effect.flatMap((0, exports.make)(), (self) => (0, exports.runtime)(self)());
exports.makeRuntime = makeRuntime;
const makeRuntimePromise = () => Effect.flatMap((0, exports.make)(), (self) => (0, exports.runtimePromise)(self)());
exports.makeRuntimePromise = makeRuntimePromise;
const internalFiberIdId = -1;
const internalFiberId = FiberId.make(internalFiberIdId, 0);
const isInternalInterruption = Cause.reduceWithContext(undefined, {
    emptyCase: Function_js_1.constFalse,
    failCase: Function_js_1.constFalse,
    dieCase: Function_js_1.constFalse,
    interruptCase: (_, fiberId) => HashSet.has(FiberId.ids(fiberId), internalFiberIdId),
    sequentialCase: (_, left, right) => left || right,
    parallelCase: (_, left, right) => left || right
});
exports.unsafeSet = (0, Function_js_1.dual)((args) => (0, exports.isFiberHandle)(args[0]), (self, fiber, options) => {
    if (self.state._tag === "Closed") {
        fiber.unsafeInterruptAsFork(FiberId.combine(options?.interruptAs ?? FiberId.none, internalFiberId));
        return;
    }
    else if (self.state.fiber !== undefined) {
        if (options?.onlyIfMissing === true) {
            fiber.unsafeInterruptAsFork(FiberId.combine(options?.interruptAs ?? FiberId.none, internalFiberId));
            return;
        }
        else if (self.state.fiber === fiber) {
            return;
        }
        self.state.fiber.unsafeInterruptAsFork(FiberId.combine(options?.interruptAs ?? FiberId.none, internalFiberId));
        self.state.fiber = undefined;
    }
    self.state.fiber = fiber;
    fiber.addObserver((exit) => {
        if (self.state._tag === "Open" && fiber === self.state.fiber) {
            self.state.fiber = undefined;
        }
        if (Exit.isFailure(exit) &&
            (options?.propagateInterruption === true ?
                !isInternalInterruption(exit.cause) :
                !Cause.isInterruptedOnly(exit.cause))) {
            Deferred.unsafeDone(self.deferred, exit);
        }
    });
});
exports.set = (0, Function_js_1.dual)((args) => (0, exports.isFiberHandle)(args[0]), (self, fiber, options) => Effect.fiberIdWith((fiberId) => Effect.sync(() => (0, exports.unsafeSet)(self, fiber, {
    interruptAs: fiberId,
    onlyIfMissing: options?.onlyIfMissing,
    propagateInterruption: options?.propagateInterruption
}))));
const unsafeGet = (self) => self.state._tag === "Closed" ? Option.none() : Option.fromNullable(self.state.fiber);
exports.unsafeGet = unsafeGet;
const get = (self) => Effect.suspend(() => (0, exports.unsafeGet)(self));
exports.get = get;
const clear = (self) => Effect.uninterruptibleMask((restore) => Effect.withFiberRuntime((fiber) => {
    if (self.state._tag === "Closed" || self.state.fiber === undefined) {
        return Effect.void;
    }
    return Effect.zipRight(restore(Fiber.interruptAs(self.state.fiber, FiberId.combine(fiber.id(), internalFiberId))), Effect.sync(() => {
        if (self.state._tag === "Open") {
            self.state.fiber = undefined;
        }
    }));
}));
exports.clear = clear;
const constInterruptedFiber = (function () {
    let fiber = undefined;
    return () => {
        if (fiber === undefined) {
            fiber = Effect.runFork(Effect.interrupt);
        }
        return fiber;
    };
})();
const run = function () {
    const self = arguments[0];
    if (Effect.isEffect(arguments[1])) {
        return runImpl(self, arguments[1], arguments[2]);
    }
    const options = arguments[1];
    return (effect) => runImpl(self, effect, options);
};
exports.run = run;
const runImpl = (self, effect, options) => Effect.withFiberRuntime((parent) => {
    if (self.state._tag === "Closed") {
        return Effect.interrupt;
    }
    else if (self.state.fiber !== undefined && options?.onlyIfMissing === true) {
        return Effect.sync(constInterruptedFiber);
    }
    const runtime = Runtime.make({
        context: parent.currentContext,
        fiberRefs: parent.getFiberRefs(),
        runtimeFlags: Runtime.defaultRuntime.runtimeFlags
    });
    const fiber = Runtime.runFork(runtime)(effect);
    (0, exports.unsafeSet)(self, fiber, { ...options, interruptAs: parent.id() });
    return Effect.succeed(fiber);
});
const runtime = (self) => () => Effect.map(Effect.runtime(), (runtime) => {
    const runFork = Runtime.runFork(runtime);
    return (effect, options) => {
        if (self.state._tag === "Closed") {
            return constInterruptedFiber();
        }
        else if (self.state.fiber !== undefined && options?.onlyIfMissing === true) {
            return constInterruptedFiber();
        }
        const fiber = runFork(effect, options);
        (0, exports.unsafeSet)(self, fiber, options);
        return fiber;
    };
});
exports.runtime = runtime;
const runtimePromise = (self) => () => Effect.map((0, exports.runtime)(self)(), (runFork) => (effect, options) => new Promise((resolve, reject) => runFork(effect, options).addObserver((exit) => {
    if (Exit.isSuccess(exit)) {
        resolve(exit.value);
    }
    else {
        reject(Cause.squash(exit.cause));
    }
})));
exports.runtimePromise = runtimePromise;
const join = (self) => Deferred.await(self.deferred);
exports.join = join;
const awaitEmpty = (self) => Effect.suspend(() => {
    if (self.state._tag === "Closed" || self.state.fiber === undefined) {
        return Effect.void;
    }
    return Fiber.await(self.state.fiber);
});
exports.awaitEmpty = awaitEmpty;
//# sourceMappingURL=FiberHandle.js.map
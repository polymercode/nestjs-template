"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.toArray = exports.toChunk = exports.takeUpTo = exports.takeOption = exports.takeAll = exports.take = exports.size = exports.retainIf = exports.removeIf = exports.peekOption = exports.peek = exports.offerAll = exports.offer = exports.make = exports.isNonEmpty = exports.isEmpty = exports.fromIterable = exports.empty = exports.TPriorityQueueImpl = exports.TPriorityQueueTypeId = void 0;
const Arr = __importStar(require("../../Array.js"));
const Chunk = __importStar(require("../../Chunk.js"));
const Function_js_1 = require("../../Function.js");
const Option = __importStar(require("../../Option.js"));
const SortedMap = __importStar(require("../../SortedMap.js"));
const core = __importStar(require("./core.js"));
const tRef = __importStar(require("./tRef.js"));
const TPriorityQueueSymbolKey = "effect/TPriorityQueue";
exports.TPriorityQueueTypeId = Symbol.for(TPriorityQueueSymbolKey);
const tPriorityQueueVariance = {
    _A: (_) => _
};
class TPriorityQueueImpl {
    ref;
    [exports.TPriorityQueueTypeId] = tPriorityQueueVariance;
    constructor(ref) {
        this.ref = ref;
    }
}
exports.TPriorityQueueImpl = TPriorityQueueImpl;
const empty = (order) => (0, Function_js_1.pipe)(tRef.make(SortedMap.empty(order)), core.map((ref) => new TPriorityQueueImpl(ref)));
exports.empty = empty;
const fromIterable = (order) => (iterable) => (0, Function_js_1.pipe)(tRef.make(Arr.fromIterable(iterable).reduce((map, value) => (0, Function_js_1.pipe)(map, SortedMap.set(value, (0, Function_js_1.pipe)(map, SortedMap.get(value), Option.match({
    onNone: () => Arr.of(value),
    onSome: Arr.prepend(value)
})))), SortedMap.empty(order))), core.map((ref) => new TPriorityQueueImpl(ref)));
exports.fromIterable = fromIterable;
const isEmpty = (self) => core.map(tRef.get(self.ref), SortedMap.isEmpty);
exports.isEmpty = isEmpty;
const isNonEmpty = (self) => core.map(tRef.get(self.ref), SortedMap.isNonEmpty);
exports.isNonEmpty = isNonEmpty;
const make = (order) => (...elements) => (0, exports.fromIterable)(order)(elements);
exports.make = make;
exports.offer = (0, Function_js_1.dual)(2, (self, value) => tRef.update(self.ref, (map) => SortedMap.set(map, value, Option.match(SortedMap.get(map, value), {
    onNone: () => Arr.of(value),
    onSome: Arr.prepend(value)
}))));
exports.offerAll = (0, Function_js_1.dual)(2, (self, values) => tRef.update(self.ref, (map) => Arr.fromIterable(values).reduce((map, value) => SortedMap.set(map, value, Option.match(SortedMap.get(map, value), {
    onNone: () => Arr.of(value),
    onSome: Arr.prepend(value)
})), map)));
const peek = (self) => core.withSTMRuntime((runtime) => {
    const map = tRef.unsafeGet(self.ref, runtime.journal);
    return Option.match(SortedMap.headOption(map), {
        onNone: () => core.retry,
        onSome: (elements) => core.succeed(elements[0])
    });
});
exports.peek = peek;
const peekOption = (self) => tRef.modify(self.ref, (map) => [
    Option.map(SortedMap.headOption(map), (elements) => elements[0]),
    map
]);
exports.peekOption = peekOption;
exports.removeIf = (0, Function_js_1.dual)(2, (self, predicate) => (0, exports.retainIf)(self, (a) => !predicate(a)));
exports.retainIf = (0, Function_js_1.dual)(2, (self, predicate) => tRef.update(self.ref, (map) => SortedMap.reduce(map, SortedMap.empty(SortedMap.getOrder(map)), (map, value, key) => {
    const filtered = Arr.filter(value, predicate);
    return filtered.length > 0 ?
        SortedMap.set(map, key, filtered) :
        SortedMap.remove(map, key);
})));
const size = (self) => tRef.modify(self.ref, (map) => [SortedMap.reduce(map, 0, (n, as) => n + as.length), map]);
exports.size = size;
const take = (self) => core.withSTMRuntime((runtime) => {
    const map = tRef.unsafeGet(self.ref, runtime.journal);
    return Option.match(SortedMap.headOption(map), {
        onNone: () => core.retry,
        onSome: (values) => {
            const head = values[1][0];
            const tail = values[1].slice(1);
            tRef.unsafeSet(self.ref, tail.length > 0 ?
                SortedMap.set(map, head, tail) :
                SortedMap.remove(map, head), runtime.journal);
            return core.succeed(head);
        }
    });
});
exports.take = take;
const takeAll = (self) => tRef.modify(self.ref, (map) => {
    const builder = [];
    for (const entry of map) {
        for (const value of entry[1]) {
            builder.push(value);
        }
    }
    return [builder, SortedMap.empty(SortedMap.getOrder(map))];
});
exports.takeAll = takeAll;
const takeOption = (self) => core.effect((journal) => {
    const map = (0, Function_js_1.pipe)(self.ref, tRef.unsafeGet(journal));
    return Option.match(SortedMap.headOption(map), {
        onNone: () => Option.none(),
        onSome: ([key, value]) => {
            const tail = value.slice(1);
            tRef.unsafeSet(self.ref, tail.length > 0 ?
                SortedMap.set(map, key, tail) :
                SortedMap.remove(map, key), journal);
            return Option.some(value[0]);
        }
    });
});
exports.takeOption = takeOption;
exports.takeUpTo = (0, Function_js_1.dual)(2, (self, n) => tRef.modify(self.ref, (map) => {
    const builder = [];
    const iterator = map[Symbol.iterator]();
    let updated = map;
    let index = 0;
    let next;
    while ((next = iterator.next()) && !next.done && index < n) {
        const [key, value] = next.value;
        const [left, right] = (0, Function_js_1.pipe)(value, Arr.splitAt(n - index));
        for (const value of left) {
            builder.push(value);
        }
        if (right.length > 0) {
            updated = SortedMap.set(updated, key, right);
        }
        else {
            updated = SortedMap.remove(updated, key);
        }
        index = index + left.length;
    }
    return [builder, updated];
}));
const toChunk = (self) => tRef.modify(self.ref, (map) => {
    const builder = [];
    for (const entry of map) {
        for (const value of entry[1]) {
            builder.push(value);
        }
    }
    return [Chunk.unsafeFromArray(builder), map];
});
exports.toChunk = toChunk;
const toArray = (self) => tRef.modify(self.ref, (map) => {
    const builder = [];
    for (const entry of map) {
        for (const value of entry[1]) {
            builder.push(value);
        }
    }
    return [builder, map];
});
exports.toArray = toArray;
//# sourceMappingURL=tPriorityQueue.js.map
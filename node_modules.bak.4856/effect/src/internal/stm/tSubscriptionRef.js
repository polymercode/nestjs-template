"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.changesStream = exports.changesScoped = exports.updateSomeAndGet = exports.updateSome = exports.updateAndGet = exports.update = exports.modifySome = exports.modify = exports.setAndGet = exports.getAndUpdateSome = exports.getAndUpdate = exports.getAndSet = exports.set = exports.get = exports.make = exports.TSubscriptionRefTypeId = void 0;
const Effect = __importStar(require("../../Effect.js"));
const Function_js_1 = require("../../Function.js");
const Option = __importStar(require("../../Option.js"));
const Pipeable_js_1 = require("../../Pipeable.js");
const STM = __importStar(require("../../STM.js"));
const TPubSub = __importStar(require("../../TPubSub.js"));
const TQueue = __importStar(require("../../TQueue.js"));
const TRef = __importStar(require("../../TRef.js"));
const stream = __importStar(require("../stream.js"));
const tQueue_js_1 = require("./tQueue.js");
const tRef_js_1 = require("./tRef.js");
const TSubscriptionRefSymbolKey = "effect/TSubscriptionRef";
exports.TSubscriptionRefTypeId = Symbol.for(TSubscriptionRefSymbolKey);
const TSubscriptionRefVariance = {
    _A: (_) => _
};
class TDequeueMerge {
    first;
    second;
    [TQueue.TDequeueTypeId] = tQueue_js_1.tDequeueVariance;
    constructor(first, second) {
        this.first = first;
        this.second = second;
    }
    peek = STM.gen(this, function* () {
        const first = yield* this.peekOption;
        if (first._tag === "Some") {
            return first.value;
        }
        return yield* STM.retry;
    });
    peekOption = STM.gen(this, function* () {
        const first = yield* this.first.peekOption;
        if (first._tag === "Some") {
            return first;
        }
        const second = yield* this.second.peekOption;
        if (second._tag === "Some") {
            return second;
        }
        return Option.none();
    });
    take = STM.gen(this, function* () {
        if (!(yield* this.first.isEmpty)) {
            return yield* this.first.take;
        }
        if (!(yield* this.second.isEmpty)) {
            return yield* this.second.take;
        }
        return yield* STM.retry;
    });
    takeAll = STM.gen(this, function* () {
        return [...yield* this.first.takeAll, ...yield* this.second.takeAll];
    });
    takeUpTo(max) {
        return STM.gen(this, function* () {
            const first = yield* this.first.takeUpTo(max);
            if (first.length >= max) {
                return first;
            }
            return [...first, ...yield* this.second.takeUpTo(max - first.length)];
        });
    }
    capacity() {
        return this.first.capacity() + this.second.capacity();
    }
    size = STM.gen(this, function* () {
        return (yield* this.first.size) + (yield* this.second.size);
    });
    isFull = STM.gen(this, function* () {
        return (yield* this.first.isFull) && (yield* this.second.isFull);
    });
    isEmpty = STM.gen(this, function* () {
        return (yield* this.first.isEmpty) && (yield* this.second.isEmpty);
    });
    shutdown = STM.gen(this, function* () {
        yield* this.first.shutdown;
        yield* this.second.shutdown;
    });
    isShutdown = STM.gen(this, function* () {
        return (yield* this.first.isShutdown) && (yield* this.second.isShutdown);
    });
    awaitShutdown = STM.gen(this, function* () {
        yield* this.first.awaitShutdown;
        yield* this.second.awaitShutdown;
    });
}
class TSubscriptionRefImpl {
    ref;
    pubsub;
    [exports.TSubscriptionRefTypeId] = TSubscriptionRefVariance;
    [TRef.TRefTypeId] = tRef_js_1.tRefVariance;
    constructor(ref, pubsub) {
        this.ref = ref;
        this.pubsub = pubsub;
    }
    get todos() {
        return this.ref.todos;
    }
    get versioned() {
        return this.ref.versioned;
    }
    pipe() {
        return (0, Pipeable_js_1.pipeArguments)(this, arguments);
    }
    get changes() {
        return STM.gen(this, function* () {
            const first = yield* TQueue.unbounded();
            yield* TQueue.offer(first, yield* TRef.get(this.ref));
            return new TDequeueMerge(first, yield* TPubSub.subscribe(this.pubsub));
        });
    }
    modify(f) {
        return (0, Function_js_1.pipe)(TRef.get(this.ref), STM.map(f), STM.flatMap(([b, a]) => (0, Function_js_1.pipe)(TRef.set(this.ref, a), STM.as(b), STM.zipLeft(TPubSub.publish(this.pubsub, a)))));
    }
}
const make = (value) => (0, Function_js_1.pipe)(STM.all([
    TPubSub.unbounded(),
    TRef.make(value)
]), STM.map(([pubsub, ref]) => new TSubscriptionRefImpl(ref, pubsub)));
exports.make = make;
const get = (self) => TRef.get(self.ref);
exports.get = get;
exports.set = (0, Function_js_1.dual)(2, (self, value) => self.modify(() => [void 0, value]));
exports.getAndSet = (0, Function_js_1.dual)(2, (self, value) => self.modify((a) => [a, value]));
exports.getAndUpdate = (0, Function_js_1.dual)(2, (self, f) => self.modify((a) => [a, f(a)]));
exports.getAndUpdateSome = (0, Function_js_1.dual)(2, (self, f) => self.modify((a) => Option.match(f(a), {
    onNone: () => [a, a],
    onSome: (b) => [a, b]
})));
exports.setAndGet = (0, Function_js_1.dual)(2, (self, value) => self.modify(() => [value, value]));
exports.modify = (0, Function_js_1.dual)(2, (self, f) => self.modify(f));
exports.modifySome = (0, Function_js_1.dual)(3, (self, fallback, f) => self.modify((a) => Option.match(f(a), {
    onNone: () => [fallback, a],
    onSome: (b) => b
})));
exports.update = (0, Function_js_1.dual)(2, (self, f) => self.modify((a) => [void 0, f(a)]));
exports.updateAndGet = (0, Function_js_1.dual)(2, (self, f) => self.modify((a) => {
    const b = f(a);
    return [b, b];
}));
exports.updateSome = (0, Function_js_1.dual)(2, (self, f) => self.modify((a) => [
    void 0,
    Option.match(f(a), {
        onNone: () => a,
        onSome: (b) => b
    })
]));
exports.updateSomeAndGet = (0, Function_js_1.dual)(2, (self, f) => self.modify((a) => Option.match(f(a), {
    onNone: () => [a, a],
    onSome: (b) => [b, b]
})));
const changesScoped = (self) => Effect.acquireRelease(self.changes, TQueue.shutdown);
exports.changesScoped = changesScoped;
const changesStream = (self) => stream.unwrap(Effect.map(self.changes, stream.fromTQueue));
exports.changesStream = changesStream;
//# sourceMappingURL=tSubscriptionRef.js.map
import type * as Cache from "../Cache.js";
import * as Duration from "../Duration.js";
import type * as Effect from "../Effect.js";
import * as Exit from "../Exit.js";
import * as MutableHashMap from "../MutableHashMap.js";
import * as MutableQueue from "../MutableQueue.js";
import * as MutableRef from "../MutableRef.js";
import * as Scope from "../Scope.js";
import type * as ScopedCache from "../ScopedCache.js";
import * as cache_ from "./cache.js";
export interface CacheState<in out Key, out Value, out Error = never> {
    map: MutableHashMap.MutableHashMap<Key, MapValue<Key, Value, Error>>;
    keys: cache_.KeySet<Key>;
    accesses: MutableQueue.MutableQueue<cache_.MapKey<Key>>;
    updating: MutableRef.MutableRef<boolean>;
    hits: number;
    misses: number;
}
export declare const makeCacheState: <Key, Value, Error = never>(map: MutableHashMap.MutableHashMap<Key, MapValue<Key, Value, Error>>, keys: cache_.KeySet<Key>, accesses: MutableQueue.MutableQueue<cache_.MapKey<Key>>, updating: MutableRef.MutableRef<boolean>, hits: number, misses: number) => CacheState<Key, Value, Error>;
export declare const initialCacheState: <Key, Value, Error = never>() => CacheState<Key, Value, Error>;
export type MapValue<Key, Value, Error> = Complete<Key, Value, Error> | Pending<Key, Value, Error> | Refreshing<Key, Value, Error>;
export interface Complete<out Key, out Value, out Error> {
    readonly _tag: "Complete";
    readonly key: cache_.MapKey<Key>;
    readonly exit: Exit.Exit<readonly [Value, Scope.Scope.Finalizer], Error>;
    readonly ownerCount: MutableRef.MutableRef<number>;
    readonly entryStats: Cache.EntryStats;
    readonly timeToLive: number;
}
export interface Pending<out Key, out Value, out Error> {
    readonly _tag: "Pending";
    readonly key: cache_.MapKey<Key>;
    readonly scoped: Effect.Effect<Effect.Effect<Value, Error, Scope.Scope>>;
}
export interface Refreshing<out Key, out Value, out Error> {
    readonly _tag: "Refreshing";
    readonly scoped: Effect.Effect<Effect.Effect<Value, Error, Scope.Scope>>;
    readonly complete: Complete<Key, Value, Error>;
}
export declare const complete: <Key, Value, Error = never>(key: cache_.MapKey<Key>, exit: Exit.Exit<readonly [Value, Scope.Scope.Finalizer], Error>, ownerCount: MutableRef.MutableRef<number>, entryStats: Cache.EntryStats, timeToLive: number) => Complete<Key, Value, Error>;
export declare const pending: <Key, Value, Error = never>(key: cache_.MapKey<Key>, scoped: Effect.Effect<Effect.Effect<Value, Error, Scope.Scope>>) => Pending<Key, Value, Error>;
export declare const refreshing: <Key, Value, Error = never>(scoped: Effect.Effect<Effect.Effect<Value, Error, Scope.Scope>>, complete: Complete<Key, Value, Error>) => Refreshing<Key, Value, Error>;
export declare const toScoped: <Key, Value, Error = never>(self: Complete<Key, Value, Error>) => Effect.Effect<Value, Error, Scope.Scope>;
export declare const releaseOwner: <Key, Value, Error = never>(self: Complete<Key, Value, Error>) => Effect.Effect<void>;
export declare const ScopedCacheTypeId: ScopedCache.ScopedCacheTypeId;
export declare const make: <Key, Value, Error = never, Environment = never>(options: {
    readonly lookup: ScopedCache.Lookup<Key, Value, Error, Environment>;
    readonly capacity: number;
    readonly timeToLive: Duration.DurationInput;
}) => Effect.Effect<ScopedCache.ScopedCache<Key, Value, Error>, never, Environment | Scope.Scope>;
export declare const makeWith: <Key, Value, Error = never, Environment = never>(options: {
    readonly capacity: number;
    readonly lookup: ScopedCache.Lookup<Key, Value, Error, Environment>;
    readonly timeToLive: (exit: Exit.Exit<Value, Error>) => Duration.DurationInput;
}) => Effect.Effect<ScopedCache.ScopedCache<Key, Value, Error>, never, Environment | Scope.Scope>;

"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatPath = exports.formatPathKey = exports.isSingle = exports.isNonEmpty = exports.memoizeThunk = exports.getKeysForIndexSignature = void 0;
const Inspectable = __importStar(require("../../Inspectable.js"));
const getKeysForIndexSignature = (input, parameter) => {
    switch (parameter._tag) {
        case "StringKeyword":
        case "TemplateLiteral":
            return Object.keys(input);
        case "SymbolKeyword":
            return Object.getOwnPropertySymbols(input);
        case "Refinement":
            return (0, exports.getKeysForIndexSignature)(input, parameter.from);
    }
};
exports.getKeysForIndexSignature = getKeysForIndexSignature;
const memoizeThunk = (f) => {
    let done = false;
    let a;
    return () => {
        if (done) {
            return a;
        }
        a = f();
        done = true;
        return a;
    };
};
exports.memoizeThunk = memoizeThunk;
const isNonEmpty = (x) => Array.isArray(x);
exports.isNonEmpty = isNonEmpty;
const isSingle = (x) => !Array.isArray(x);
exports.isSingle = isSingle;
const formatPathKey = (key) => `[${Inspectable.formatPropertyKey(key)}]`;
exports.formatPathKey = formatPathKey;
const formatPath = (path) => (0, exports.isNonEmpty)(path) ? path.map(exports.formatPathKey).join("") : (0, exports.formatPathKey)(path);
exports.formatPath = formatPath;
//# sourceMappingURL=util.js.map
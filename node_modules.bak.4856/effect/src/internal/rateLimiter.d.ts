import * as Effect from "../Effect.js";
import type * as RateLimiter from "../RateLimiter.js";
import type * as Scope from "../Scope.js";
export declare const make: ({ algorithm, interval, limit }: RateLimiter.RateLimiter.Options) => Effect.Effect<RateLimiter.RateLimiter, never, Scope.Scope>;
export declare const withCost: (cost: number) => <B, E, R>(use: Effect.Effect<B, E, R>) => Effect.Effect<B, E, R>;

import type * as Effect from "../Effect.js";
import type { LazyArg } from "../Function.js";
import type * as Scope from "../Scope.js";
import type * as ScopedRef from "../ScopedRef.js";
export declare const ScopedRefTypeId: ScopedRef.ScopedRefTypeId;
export declare const fromAcquire: <A, E, R>(acquire: Effect.Effect<A, E, R>) => Effect.Effect<ScopedRef.ScopedRef<A>, E, R | Scope.Scope>;
export declare const get: <A>(self: ScopedRef.ScopedRef<A>) => Effect.Effect<A>;
export declare const make: <A>(evaluate: LazyArg<A>) => Effect.Effect<ScopedRef.ScopedRef<A>, never, Scope.Scope>;
export declare const set: (<A, R, E>(acquire: Effect.Effect<A, E, R>) => (self: ScopedRef.ScopedRef<A>) => Effect.Effect<void, E, Exclude<R, Scope.Scope>>) & (<A, R, E>(self: ScopedRef.ScopedRef<A>, acquire: Effect.Effect<A, E, R>) => Effect.Effect<void, E, Exclude<R, Scope.Scope>>);

"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.set = exports.make = exports.get = exports.fromAcquire = exports.ScopedRefTypeId = void 0;
const Context = __importStar(require("../Context.js"));
const Function_js_1 = require("../Function.js");
const core = __importStar(require("./core.js"));
const circular = __importStar(require("./effect/circular.js"));
const effectable = __importStar(require("./effectable.js"));
const fiberRuntime = __importStar(require("./fiberRuntime.js"));
const ref = __importStar(require("./ref.js"));
const synchronized = __importStar(require("./synchronizedRef.js"));
const ScopedRefSymbolKey = "effect/ScopedRef";
exports.ScopedRefTypeId = Symbol.for(ScopedRefSymbolKey);
const scopedRefVariance = {
    _A: (_) => _
};
const proto = {
    ...effectable.CommitPrototype,
    commit() {
        return (0, exports.get)(this);
    },
    [exports.ScopedRefTypeId]: scopedRefVariance
};
const close = (self) => core.flatMap(ref.get(self.ref), (tuple) => tuple[0].close(core.exitVoid));
const fromAcquire = (acquire) => core.uninterruptible(fiberRuntime.scopeMake().pipe(core.flatMap((newScope) => acquire.pipe(core.mapInputContext(Context.add(fiberRuntime.scopeTag, newScope)), core.onError((cause) => newScope.close(core.exitFail(cause))), core.flatMap((value) => circular.makeSynchronized([newScope, value]).pipe(core.flatMap((ref) => {
    const scopedRef = Object.create(proto);
    scopedRef.ref = ref;
    return (0, Function_js_1.pipe)(fiberRuntime.addFinalizer(() => close(scopedRef)), core.as(scopedRef));
})))))));
exports.fromAcquire = fromAcquire;
const get = (self) => core.map(ref.get(self.ref), (tuple) => tuple[1]);
exports.get = get;
const make = (evaluate) => (0, exports.fromAcquire)(core.sync(evaluate));
exports.make = make;
exports.set = (0, Function_js_1.dual)(2, (self, acquire) => core.flatten(synchronized.modifyEffect(self.ref, ([oldScope, value]) => core.uninterruptible(core.scopeClose(oldScope, core.exitVoid).pipe(core.zipRight(fiberRuntime.scopeMake()), core.flatMap((newScope) => core.exit(fiberRuntime.scopeExtend(acquire, newScope)).pipe(core.flatMap((exit) => core.exitMatch(exit, {
    onFailure: (cause) => core.scopeClose(newScope, core.exitVoid).pipe(core.as([
        core.failCause(cause),
        [oldScope, value]
    ])),
    onSuccess: (value) => core.succeed([
        core.void,
        [newScope, value]
    ])
})))))))));
//# sourceMappingURL=scopedRef.js.map
import * as Duration from "../Duration.js";
import type * as Effect from "../Effect.js";
import type * as KeyedPool from "../KeyedPool.js";
import type * as Scope from "../Scope.js";
export declare const KeyedPoolTypeId: KeyedPool.KeyedPoolTypeId;
export declare const make: <K, A, E, R>(options: {
    readonly acquire: (key: K) => Effect.Effect<A, E, R>;
    readonly size: number;
}) => Effect.Effect<KeyedPool.KeyedPool<K, A, E>, never, R | Scope.Scope>;
export declare const makeWith: <K, A, E, R>(options: {
    readonly acquire: (key: K) => Effect.Effect<A, E, R>;
    readonly size: (key: K) => number;
}) => Effect.Effect<KeyedPool.KeyedPool<K, A, E>, never, R | Scope.Scope>;
export declare const makeWithTTL: <K, A, E, R>(options: {
    readonly acquire: (key: K) => Effect.Effect<A, E, R>;
    readonly min: (key: K) => number;
    readonly max: (key: K) => number;
    readonly timeToLive: Duration.DurationInput;
}) => Effect.Effect<KeyedPool.KeyedPool<K, A, E>, never, R | Scope.Scope>;
export declare const makeWithTTLBy: <K, A, E, R>(options: {
    readonly acquire: (key: K) => Effect.Effect<A, E, R>;
    readonly min: (key: K) => number;
    readonly max: (key: K) => number;
    readonly timeToLive: (key: K) => Duration.DurationInput;
}) => Effect.Effect<KeyedPool.KeyedPool<K, A, E>, never, R | Scope.Scope>;
export declare const get: (<K>(key: K) => <A, E>(self: KeyedPool.KeyedPool<K, A, E>) => Effect.Effect<A, E, Scope.Scope>) & (<K, A, E>(self: KeyedPool.KeyedPool<K, A, E>, key: K) => Effect.Effect<A, E, Scope.Scope>);
export declare const invalidate: (<A>(item: A) => <K, E>(self: KeyedPool.KeyedPool<K, A, E>) => Effect.Effect<void>) & (<K, A, E>(self: KeyedPool.KeyedPool<K, A, E>, item: A) => Effect.Effect<void>);

"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.longestPrefixOf = exports.modify = exports.insertMany = exports.removeMany = exports.remove = exports.unsafeGet = exports.has = exports.get = exports.toEntriesWithPrefix = exports.entriesWithPrefix = exports.valuesWithPrefix = exports.keysWithPrefix = exports.forEach = exports.compact = exports.filterMap = exports.filter = exports.map = exports.reduce = exports.entries = exports.values = exports.keys = exports.isEmpty = exports.size = exports.insert = exports.make = exports.fromIterable = exports.empty = exports.isTrie = exports.TrieTypeId = void 0;
const Equal = __importStar(require("../Equal.js"));
const Function_js_1 = require("../Function.js");
const Hash = __importStar(require("../Hash.js"));
const Inspectable_js_1 = require("../Inspectable.js");
const Option = __importStar(require("../Option.js"));
const Pipeable_js_1 = require("../Pipeable.js");
const Predicate_js_1 = require("../Predicate.js");
const TrieSymbolKey = "effect/Trie";
exports.TrieTypeId = Symbol.for(TrieSymbolKey);
const trieVariance = {
    _Value: (_) => _
};
const TrieProto = {
    [exports.TrieTypeId]: trieVariance,
    [Symbol.iterator]() {
        return new TrieIterator(this, (k, v) => [k, v], () => true);
    },
    [Hash.symbol]() {
        let hash = Hash.hash(TrieSymbolKey);
        for (const item of this) {
            hash ^= (0, Function_js_1.pipe)(Hash.hash(item[0]), Hash.combine(Hash.hash(item[1])));
        }
        return Hash.cached(this, hash);
    },
    [Equal.symbol](that) {
        if ((0, exports.isTrie)(that)) {
            const entries = Array.from(that);
            return Array.from(this).every((itemSelf, i) => {
                const itemThat = entries[i];
                return Equal.equals(itemSelf[0], itemThat[0]) && Equal.equals(itemSelf[1], itemThat[1]);
            });
        }
        return false;
    },
    toString() {
        return (0, Inspectable_js_1.format)(this.toJSON());
    },
    toJSON() {
        return {
            _id: "Trie",
            values: Array.from(this).map(Inspectable_js_1.toJSON)
        };
    },
    [Inspectable_js_1.NodeInspectSymbol]() {
        return this.toJSON();
    },
    pipe() {
        return (0, Pipeable_js_1.pipeArguments)(this, arguments);
    }
};
const makeImpl = (root) => {
    const trie = Object.create(TrieProto);
    trie._root = root;
    trie._count = root?.count ?? 0;
    return trie;
};
class TrieIterator {
    trie;
    f;
    filter;
    stack = [];
    constructor(trie, f, filter) {
        this.trie = trie;
        this.f = f;
        this.filter = filter;
        const root = trie._root !== undefined ? trie._root : undefined;
        if (root !== undefined) {
            this.stack.push([root, "", false]);
        }
    }
    next() {
        while (this.stack.length > 0) {
            const [node, keyString, isAdded] = this.stack.pop();
            if (isAdded) {
                const value = node.value;
                if (value !== undefined) {
                    const key = keyString + node.key;
                    if (this.filter(key, value)) {
                        return { done: false, value: this.f(key, value) };
                    }
                }
            }
            else {
                this.addToStack(node, keyString);
            }
        }
        return { done: true, value: undefined };
    }
    addToStack(node, keyString) {
        if (node.right !== undefined) {
            this.stack.push([node.right, keyString, false]);
        }
        if (node.mid !== undefined) {
            this.stack.push([node.mid, keyString + node.key, false]);
        }
        this.stack.push([node, keyString, true]);
        if (node.left !== undefined) {
            this.stack.push([node.left, keyString, false]);
        }
    }
    [Symbol.iterator]() {
        return new TrieIterator(this.trie, this.f, this.filter);
    }
}
const isTrie = (u) => (0, Predicate_js_1.hasProperty)(u, exports.TrieTypeId);
exports.isTrie = isTrie;
const empty = () => makeImpl(undefined);
exports.empty = empty;
const fromIterable = (entries) => {
    let trie = (0, exports.empty)();
    for (const [key, value] of entries) {
        trie = (0, exports.insert)(trie, key, value);
    }
    return trie;
};
exports.fromIterable = fromIterable;
const make = (...entries) => {
    return (0, exports.fromIterable)(entries);
};
exports.make = make;
exports.insert = (0, Function_js_1.dual)(3, (self, key, value) => {
    if (key.length === 0)
        return self;
    const dStack = [];
    const nStack = [];
    let n = self._root ?? {
        key: key[0],
        count: 0
    };
    const count = n.count + 1;
    let cIndex = 0;
    while (cIndex < key.length) {
        const c = key[cIndex];
        nStack.push(n);
        if (c > n.key) {
            dStack.push(1);
            if (n.right === undefined) {
                n = { key: c, count };
            }
            else {
                n = n.right;
            }
        }
        else if (c < n.key) {
            dStack.push(-1);
            if (n.left === undefined) {
                n = { key: c, count };
            }
            else {
                n = n.left;
            }
        }
        else {
            if (cIndex === key.length - 1) {
                n.value = value;
            }
            else if (n.mid === undefined) {
                dStack.push(0);
                n = { key: key[cIndex + 1], count };
            }
            else {
                dStack.push(0);
                n = n.mid;
            }
            cIndex += 1;
        }
    }
    for (let s = nStack.length - 2; s >= 0; --s) {
        const n2 = nStack[s];
        const d = dStack[s];
        if (d === -1) {
            nStack[s] = {
                key: n2.key,
                count,
                value: n2.value,
                left: nStack[s + 1],
                mid: n2.mid,
                right: n2.right
            };
        }
        else if (d === 1) {
            nStack[s] = {
                key: n2.key,
                count,
                value: n2.value,
                left: n2.left,
                mid: n2.mid,
                right: nStack[s + 1]
            };
        }
        else {
            nStack[s] = {
                key: n2.key,
                count,
                value: n2.value,
                left: n2.left,
                mid: nStack[s + 1],
                right: n2.right
            };
        }
    }
    nStack[0].count = count;
    return makeImpl(nStack[0]);
});
const size = (self) => self._root?.count ?? 0;
exports.size = size;
const isEmpty = (self) => (0, exports.size)(self) === 0;
exports.isEmpty = isEmpty;
const keys = (self) => new TrieIterator(self, (key) => key, () => true);
exports.keys = keys;
const values = (self) => new TrieIterator(self, (_, value) => value, () => true);
exports.values = values;
const entries = (self) => new TrieIterator(self, (key, value) => [key, value], () => true);
exports.entries = entries;
exports.reduce = (0, Function_js_1.dual)(3, (self, zero, f) => {
    let accumulator = zero;
    for (const entry of self) {
        accumulator = f(accumulator, entry[1], entry[0]);
    }
    return accumulator;
});
exports.map = (0, Function_js_1.dual)(2, (self, f) => (0, exports.reduce)(self, (0, exports.empty)(), (trie, value, key) => (0, exports.insert)(trie, key, f(value, key))));
exports.filter = (0, Function_js_1.dual)(2, (self, f) => (0, exports.reduce)(self, (0, exports.empty)(), (trie, value, key) => f(value, key) ? (0, exports.insert)(trie, key, value) : trie));
exports.filterMap = (0, Function_js_1.dual)(2, (self, f) => (0, exports.reduce)(self, (0, exports.empty)(), (trie, value, key) => {
    const option = f(value, key);
    return Option.isSome(option) ? (0, exports.insert)(trie, key, option.value) : trie;
}));
const compact = (self) => (0, exports.filterMap)(self, Function_js_1.identity);
exports.compact = compact;
exports.forEach = (0, Function_js_1.dual)(2, (self, f) => (0, exports.reduce)(self, void 0, (_, value, key) => f(value, key)));
exports.keysWithPrefix = (0, Function_js_1.dual)(2, (self, prefix) => new TrieIterator(self, (key) => key, (key) => key.startsWith(prefix)));
exports.valuesWithPrefix = (0, Function_js_1.dual)(2, (self, prefix) => new TrieIterator(self, (_, value) => value, (key) => key.startsWith(prefix)));
exports.entriesWithPrefix = (0, Function_js_1.dual)(2, (self, prefix) => new TrieIterator(self, (key, value) => [key, value], (key) => key.startsWith(prefix)));
exports.toEntriesWithPrefix = (0, Function_js_1.dual)(2, (self, prefix) => Array.from((0, exports.entriesWithPrefix)(self, prefix)));
exports.get = (0, Function_js_1.dual)(2, (self, key) => {
    let n = self._root;
    if (n === undefined || key.length === 0)
        return Option.none();
    let cIndex = 0;
    while (cIndex < key.length) {
        const c = key[cIndex];
        if (c > n.key) {
            if (n.right === undefined) {
                return Option.none();
            }
            else {
                n = n.right;
            }
        }
        else if (c < n.key) {
            if (n.left === undefined) {
                return Option.none();
            }
            else {
                n = n.left;
            }
        }
        else {
            if (cIndex === key.length - 1) {
                return Option.fromNullable(n.value);
            }
            else {
                if (n.mid === undefined) {
                    return Option.none();
                }
                else {
                    n = n.mid;
                    cIndex += 1;
                }
            }
        }
    }
    return Option.none();
});
exports.has = (0, Function_js_1.dual)(2, (self, key) => Option.isSome((0, exports.get)(self, key)));
exports.unsafeGet = (0, Function_js_1.dual)(2, (self, key) => {
    const element = (0, exports.get)(self, key);
    if (Option.isNone(element)) {
        throw new Error("Expected trie to contain key");
    }
    return element.value;
});
exports.remove = (0, Function_js_1.dual)(2, (self, key) => {
    let n = self._root;
    if (n === undefined || key.length === 0)
        return self;
    const count = n.count - 1;
    const dStack = [];
    const nStack = [];
    let cIndex = 0;
    while (cIndex < key.length) {
        const c = key[cIndex];
        if (c > n.key) {
            if (n.right === undefined) {
                return self;
            }
            else {
                nStack.push(n);
                dStack.push(1);
                n = n.right;
            }
        }
        else if (c < n.key) {
            if (n.left === undefined) {
                return self;
            }
            else {
                nStack.push(n);
                dStack.push(-1);
                n = n.left;
            }
        }
        else {
            if (cIndex === key.length - 1) {
                if (n.value !== undefined) {
                    nStack.push(n);
                    dStack.push(0);
                    cIndex += 1;
                }
                else {
                    return self;
                }
            }
            else {
                if (n.mid === undefined) {
                    return self;
                }
                else {
                    nStack.push(n);
                    dStack.push(0);
                    n = n.mid;
                    cIndex += 1;
                }
            }
        }
    }
    const removeNode = nStack[nStack.length - 1];
    nStack[nStack.length - 1] = {
        key: removeNode.key,
        count,
        left: removeNode.left,
        mid: removeNode.mid,
        right: removeNode.right
    };
    for (let s = nStack.length - 2; s >= 0; --s) {
        const n2 = nStack[s];
        const d = dStack[s];
        const child = nStack[s + 1];
        const nc = child.left === undefined && child.mid === undefined && child.right === undefined ? undefined : child;
        if (d === -1) {
            nStack[s] = {
                key: n2.key,
                count,
                value: n2.value,
                left: nc,
                mid: n2.mid,
                right: n2.right
            };
        }
        else if (d === 1) {
            nStack[s] = {
                key: n2.key,
                count,
                value: n2.value,
                left: n2.left,
                mid: n2.mid,
                right: nc
            };
        }
        else {
            nStack[s] = {
                key: n2.key,
                count,
                value: n2.value,
                left: n2.left,
                mid: nc,
                right: n2.right
            };
        }
    }
    nStack[0].count = count;
    return makeImpl(nStack[0]);
});
exports.removeMany = (0, Function_js_1.dual)(2, (self, keys) => {
    let trie = self;
    for (const key of keys) {
        trie = (0, exports.remove)(key)(trie);
    }
    return trie;
});
exports.insertMany = (0, Function_js_1.dual)(2, (self, iter) => {
    let trie = self;
    for (const [key, value] of iter) {
        trie = (0, exports.insert)(key, value)(trie);
    }
    return trie;
});
exports.modify = (0, Function_js_1.dual)(3, (self, key, f) => {
    let n = self._root;
    if (n === undefined || key.length === 0)
        return self;
    const dStack = [];
    const nStack = [];
    let cIndex = 0;
    while (cIndex < key.length) {
        const c = key[cIndex];
        if (c > n.key) {
            if (n.right === undefined) {
                return self;
            }
            else {
                nStack.push(n);
                dStack.push(1);
                n = n.right;
            }
        }
        else if (c < n.key) {
            if (n.left === undefined) {
                return self;
            }
            else {
                nStack.push(n);
                dStack.push(-1);
                n = n.left;
            }
        }
        else {
            if (cIndex === key.length - 1) {
                if (n.value !== undefined) {
                    nStack.push(n);
                    dStack.push(0);
                    cIndex += 1;
                }
                else {
                    return self;
                }
            }
            else {
                if (n.mid === undefined) {
                    return self;
                }
                else {
                    nStack.push(n);
                    dStack.push(0);
                    n = n.mid;
                    cIndex += 1;
                }
            }
        }
    }
    const updateNode = nStack[nStack.length - 1];
    if (updateNode.value === undefined) {
        return self;
    }
    nStack[nStack.length - 1] = {
        key: updateNode.key,
        count: updateNode.count,
        value: f(updateNode.value),
        left: updateNode.left,
        mid: updateNode.mid,
        right: updateNode.right
    };
    for (let s = nStack.length - 2; s >= 0; --s) {
        const n2 = nStack[s];
        const d = dStack[s];
        const child = nStack[s + 1];
        if (d === -1) {
            nStack[s] = {
                key: n2.key,
                count: n2.count,
                value: n2.value,
                left: child,
                mid: n2.mid,
                right: n2.right
            };
        }
        else if (d === 1) {
            nStack[s] = {
                key: n2.key,
                count: n2.count,
                value: n2.value,
                left: n2.left,
                mid: n2.mid,
                right: child
            };
        }
        else {
            nStack[s] = {
                key: n2.key,
                count: n2.count,
                value: n2.value,
                left: n2.left,
                mid: child,
                right: n2.right
            };
        }
    }
    return makeImpl(nStack[0]);
});
exports.longestPrefixOf = (0, Function_js_1.dual)(2, (self, key) => {
    let n = self._root;
    if (n === undefined || key.length === 0)
        return Option.none();
    let longestPrefixNode = undefined;
    let cIndex = 0;
    while (cIndex < key.length) {
        const c = key[cIndex];
        if (n.value !== undefined) {
            longestPrefixNode = [key.slice(0, cIndex + 1), n.value];
        }
        if (c > n.key) {
            if (n.right === undefined) {
                break;
            }
            else {
                n = n.right;
            }
        }
        else if (c < n.key) {
            if (n.left === undefined) {
                break;
            }
            else {
                n = n.left;
            }
        }
        else {
            if (n.mid === undefined) {
                break;
            }
            else {
                n = n.mid;
                cIndex += 1;
            }
        }
    }
    return Option.fromNullable(longestPrefixNode);
});
//# sourceMappingURL=trie.js.map
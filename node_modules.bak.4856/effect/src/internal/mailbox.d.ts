import type { Channel } from "../Channel.js";
import * as Chunk from "../Chunk.js";
import type { Effect } from "../Effect.js";
import type * as Api from "../Mailbox.js";
import type { Scope } from "../Scope.js";
import type { Stream } from "../Stream.js";
export declare const TypeId: Api.TypeId;
export declare const ReadonlyTypeId: Api.ReadonlyTypeId;
export declare const isMailbox: (u: unknown) => u is Api.Mailbox<unknown, unknown>;
export declare const isReadonlyMailbox: (u: unknown) => u is Api.ReadonlyMailbox<unknown, unknown>;
export declare const make: <A, E = never>(capacity?: number | {
    readonly capacity?: number | undefined;
    readonly strategy?: "suspend" | "dropping" | "sliding" | undefined;
} | undefined) => Effect<Api.Mailbox<A, E>>;
export declare const into: {
    <A, E>(self: Api.Mailbox<A, E>): <AX, EX extends E, RX>(effect: Effect<AX, EX, RX>) => Effect<boolean, never, RX>;
    <AX, E, EX extends E, RX, A>(effect: Effect<AX, EX, RX>, self: Api.Mailbox<A, E>): Effect<boolean, never, RX>;
};
export declare const toChannel: <A, E>(self: Api.ReadonlyMailbox<A, E>) => Channel<Chunk.Chunk<A>, unknown, E>;
export declare const toStream: <A, E>(self: Api.ReadonlyMailbox<A, E>) => Stream<A, E>;
export declare const fromStream: {
    (options?: {
        readonly capacity?: number | undefined;
        readonly strategy?: "suspend" | "dropping" | "sliding" | undefined;
    }): <A, E, R>(self: Stream<A, E, R>) => Effect<Api.ReadonlyMailbox<A, E>, never, R | Scope>;
    <A, E, R>(self: Stream<A, E, R>, options?: {
        readonly capacity?: number | undefined;
        readonly strategy?: "suspend" | "dropping" | "sliding" | undefined;
    }): Effect<Api.ReadonlyMailbox<A, E>, never, R | Scope>;
};

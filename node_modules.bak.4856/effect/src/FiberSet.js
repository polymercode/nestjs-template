"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.awaitEmpty = exports.join = exports.size = exports.runtimePromise = exports.runtime = exports.run = exports.clear = exports.add = exports.unsafeAdd = exports.makeRuntimePromise = exports.makeRuntime = exports.make = exports.isFiberSet = exports.TypeId = void 0;
const Cause = __importStar(require("./Cause.js"));
const Deferred = __importStar(require("./Deferred.js"));
const Effect = __importStar(require("./Effect.js"));
const Exit = __importStar(require("./Exit.js"));
const Fiber = __importStar(require("./Fiber.js"));
const FiberId = __importStar(require("./FiberId.js"));
const Function_js_1 = require("./Function.js");
const HashSet = __importStar(require("./HashSet.js"));
const Inspectable = __importStar(require("./Inspectable.js"));
const Iterable = __importStar(require("./Iterable.js"));
const Pipeable_js_1 = require("./Pipeable.js");
const Predicate = __importStar(require("./Predicate.js"));
const Runtime = __importStar(require("./Runtime.js"));
exports.TypeId = Symbol.for("effect/FiberSet");
const isFiberSet = (u) => Predicate.hasProperty(u, exports.TypeId);
exports.isFiberSet = isFiberSet;
const Proto = {
    [exports.TypeId]: exports.TypeId,
    [Symbol.iterator]() {
        if (this.state._tag === "Closed") {
            return Iterable.empty();
        }
        return this.state.backing[Symbol.iterator]();
    },
    toString() {
        return Inspectable.format(this.toJSON());
    },
    toJSON() {
        return {
            _id: "FiberMap",
            state: this.state
        };
    },
    [Inspectable.NodeInspectSymbol]() {
        return this.toJSON();
    },
    pipe() {
        return (0, Pipeable_js_1.pipeArguments)(this, arguments);
    }
};
const unsafeMake = (backing, deferred) => {
    const self = Object.create(Proto);
    self.state = { _tag: "Open", backing };
    self.deferred = deferred;
    return self;
};
const make = () => Effect.acquireRelease(Effect.map(Deferred.make(), (deferred) => unsafeMake(new Set(), deferred)), (set) => Effect.withFiberRuntime((parent) => {
    const state = set.state;
    if (state._tag === "Closed")
        return Effect.void;
    set.state = { _tag: "Closed" };
    const fibers = state.backing;
    return Fiber.interruptAllAs(fibers, FiberId.combine(parent.id(), internalFiberId)).pipe(Effect.intoDeferred(set.deferred));
}));
exports.make = make;
const makeRuntime = () => Effect.flatMap((0, exports.make)(), (self) => (0, exports.runtime)(self)());
exports.makeRuntime = makeRuntime;
const makeRuntimePromise = () => Effect.flatMap((0, exports.make)(), (self) => (0, exports.runtimePromise)(self)());
exports.makeRuntimePromise = makeRuntimePromise;
const internalFiberIdId = -1;
const internalFiberId = FiberId.make(internalFiberIdId, 0);
const isInternalInterruption = Cause.reduceWithContext(undefined, {
    emptyCase: Function_js_1.constFalse,
    failCase: Function_js_1.constFalse,
    dieCase: Function_js_1.constFalse,
    interruptCase: (_, fiberId) => HashSet.has(FiberId.ids(fiberId), internalFiberIdId),
    sequentialCase: (_, left, right) => left || right,
    parallelCase: (_, left, right) => left || right
});
exports.unsafeAdd = (0, Function_js_1.dual)((args) => (0, exports.isFiberSet)(args[0]), (self, fiber, options) => {
    if (self.state._tag === "Closed") {
        fiber.unsafeInterruptAsFork(FiberId.combine(options?.interruptAs ?? FiberId.none, internalFiberId));
        return;
    }
    else if (self.state.backing.has(fiber)) {
        return;
    }
    self.state.backing.add(fiber);
    fiber.addObserver((exit) => {
        if (self.state._tag === "Closed") {
            return;
        }
        self.state.backing.delete(fiber);
        if (Exit.isFailure(exit) &&
            (options?.propagateInterruption === true ?
                !isInternalInterruption(exit.cause) :
                !Cause.isInterruptedOnly(exit.cause))) {
            Deferred.unsafeDone(self.deferred, exit);
        }
    });
});
exports.add = (0, Function_js_1.dual)((args) => (0, exports.isFiberSet)(args[0]), (self, fiber, options) => Effect.fiberIdWith((fiberId) => Effect.sync(() => (0, exports.unsafeAdd)(self, fiber, {
    ...options,
    interruptAs: fiberId
}))));
const clear = (self) => Effect.withFiberRuntime((clearFiber) => {
    if (self.state._tag === "Closed") {
        return Effect.void;
    }
    return Effect.forEach(self.state.backing, (fiber) => Fiber.interruptAs(fiber, FiberId.combine(clearFiber.id(), internalFiberId)));
});
exports.clear = clear;
const constInterruptedFiber = (function () {
    let fiber = undefined;
    return () => {
        if (fiber === undefined) {
            fiber = Effect.runFork(Effect.interrupt);
        }
        return fiber;
    };
})();
const run = function () {
    const self = arguments[0];
    if (!Effect.isEffect(arguments[1])) {
        const options = arguments[1];
        return (effect) => runImpl(self, effect, options);
    }
    return runImpl(self, arguments[1], arguments[2]);
};
exports.run = run;
const runImpl = (self, effect, options) => Effect.fiberIdWith((fiberId) => {
    if (self.state._tag === "Closed") {
        return Effect.sync(constInterruptedFiber);
    }
    return Effect.tap(Effect.forkDaemon(effect), (fiber) => (0, exports.unsafeAdd)(self, fiber, {
        ...options,
        interruptAs: fiberId
    }));
});
const runtime = (self) => () => Effect.map(Effect.runtime(), (runtime) => {
    const runFork = Runtime.runFork(runtime);
    return (effect, options) => {
        if (self.state._tag === "Closed") {
            return constInterruptedFiber();
        }
        const fiber = runFork(effect, options);
        (0, exports.unsafeAdd)(self, fiber);
        return fiber;
    };
});
exports.runtime = runtime;
const runtimePromise = (self) => () => Effect.map((0, exports.runtime)(self)(), (runFork) => (effect, options) => new Promise((resolve, reject) => runFork(effect, options).addObserver((exit) => {
    if (Exit.isSuccess(exit)) {
        resolve(exit.value);
    }
    else {
        reject(Cause.squash(exit.cause));
    }
})));
exports.runtimePromise = runtimePromise;
const size = (self) => Effect.sync(() => self.state._tag === "Closed" ? 0 : self.state.backing.size);
exports.size = size;
const join = (self) => Deferred.await(self.deferred);
exports.join = join;
const awaitEmpty = (self) => Effect.whileLoop({
    while: () => self.state._tag === "Open" && self.state.backing.size > 0,
    body: () => Fiber.await(Iterable.unsafeHead(self)),
    step: Function_js_1.constVoid
});
exports.awaitEmpty = awaitEmpty;
//# sourceMappingURL=FiberSet.js.map